---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import ModernCard from '../components/ui/ModernCard.astro';
import { withBasePath } from '../utils/helpers';
import LegacyRedirect from '../components/LegacyRedirect.astro';

const reasons = [
  'GitHub Pages cannot run long-lived API routes or server-rendered dashboards.',
  'Client-side framework bundles introduced unnecessary complexity for content-focused teams.',
  'Static reporting is easier to audit, translate, and preserve in version control.',
];

const alternatives = [
  'Weekly status digest template built with Markdown collections.',
  'Lightweight metric blocks on the home page that you can edit in content files.',
  'CSV-to-Markdown conversion script included in the repository utilities.',
];
---

<LegacyRedirect
  destination="/"
  reason="This page was part of the original starter template and has been removed."
/>

<!--
<!doctype html>
<html lang="en" class="dark">
  <head>
    <BaseHead
      title="Dashboard | Elite Project"
      description="Why the live dashboard was retired in favour of static, reliable reporting."
    />
  </head>
  <body class="bg-zinc-950 text-zinc-100 min-h-screen flex flex-col">
    <Header />
    <main class="flex-1">
      <section
        class="bg-zinc-950 text-zinc-50 relative overflow-hidden border-b border-zinc-800"
      >
        <div
          class="absolute inset-0 bg-[linear-gradient(to_right,#80808012_1px,transparent_1px),linear-gradient(to_bottom,#80808012_1px,transparent_1px)] bg-[size:24px_24px]"
        >
        </div>
        <div class="mx-auto max-w-4xl px-6 py-24 relative">
          <div
            class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-zinc-900 border border-zinc-800 mb-6"
          >
            <span class="w-2 h-2 rounded-full bg-red-500 animate-pulse"></span>
            <span class="text-xs font-mono text-zinc-400 tracking-wider"
              >SYSTEM_STATUS: DEPRECATED</span
            >
          </div>
          <h1
            class="mt-6 text-4xl font-display font-bold sm:text-5xl uppercase tracking-tight"
          >
            The interactive dashboard has been <span class="text-accent-500"
              >sunset</span
            >.
          </h1>
          <p class="mt-6 text-lg text-zinc-400 font-mono leading-relaxed">
            // Earlier versions of this project loaded Solid, React, Svelte, and
            Three.js just to render charts. The new release keeps everything
            static so the site builds cleanly on GitHub Pages and remains easy
            to audit.
          </p>
        </div>
      </section>

      <section class="bg-zinc-900">
        <div class="mx-auto grid max-w-4xl gap-10 px-6 py-16 sm:grid-cols-2">
          <ModernCard
            variant="minimal"
            className="bg-zinc-950 p-6 relative group hover:border-accent-500 transition-colors border-zinc-800"
          >
            <div
              class="absolute top-0 left-0 w-2 h-2 border-t border-l border-zinc-700 group-hover:border-accent-500 transition-colors"
            >
            </div>
            <h2
              class="text-xl font-display font-bold text-white uppercase tracking-wide"
            >
              Why we changed course
            </h2>
            <ul class="mt-4 space-y-3 text-sm text-zinc-400 font-mono">
              {
                reasons.map(reason => (
                  <li class="flex items-start gap-2">
                    <span class="mt-1 text-accent-500">&gt;</span>
                    <span>{reason}</span>
                  </li>
                ))
              }
            </ul>
          </ModernCard>

          <ModernCard
            variant="minimal"
            className="bg-zinc-950 p-6 relative group hover:border-accent-500 transition-colors border-zinc-800"
          >
            <div
              class="absolute top-0 right-0 w-2 h-2 border-t border-r border-zinc-700 group-hover:border-accent-500 transition-colors"
            >
            </div>
            <h2
              class="text-xl font-display font-bold text-white uppercase tracking-wide"
            >
              What to use instead
            </h2>
            <ul class="mt-4 space-y-3 text-sm text-zinc-400 font-mono">
              {
                alternatives.map(item => (
                  <li class="flex items-start gap-2">
                    <span class="mt-1 text-accent-500">&gt;</span>
                    <span>{item}</span>
                  </li>
                ))
              }
            </ul>
          </ModernCard>
        </div>
      </section>

      <section class="bg-zinc-950 text-zinc-50 border-t border-zinc-800">
        <div class="mx-auto max-w-4xl px-6 py-16 text-center">
          <h2 class="text-2xl font-display font-bold uppercase tracking-wide">
            Need guidance replicating a dashboard statically?
          </h2>
          <p class="mt-4 text-sm text-zinc-400 font-mono">
            // We can help create scheduled exports, build markdown-based status
            pages, or integrate lightweight embeds that do not break static
            hosting rules.
          </p>
          <div class="mt-6 flex flex-wrap justify-center gap-3">
            <a
              class="inline-flex items-center justify-center rounded-full bg-brand-600 px-5 py-2 text-sm font-semibold text-white transition hover:bg-brand-700"
              href={withBasePath('contact/')}
            >
              Talk with the team
            </a>
            <a
              class="inline-flex items-center justify-center rounded-full border border-neutral-400 px-5 py-2 text-sm font-semibold text-neutral-50 transition hover:border-neutral-300"
              href={withBasePath('services/')}
            >
              See service options
            </a>
          </div>
        </div>
      </section>
    </main>
    <Footer />
  </body>
</html>
-->
