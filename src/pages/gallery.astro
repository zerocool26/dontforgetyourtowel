---
import MarketingLayout from '../layouts/MarketingLayout.astro';
import StructuredData from '../components/StructuredData.astro';
import ModernButton from '../components/ui/ModernButton.astro';
import ModernCard from '../components/ui/ModernCard.astro';
import {
  galleryCategories,
  galleryInstallations,
} from '../data/design-gallery';
import { withBasePath } from '../utils/helpers';

const featuredInstallation = galleryInstallations[0];

const gallerySignals = [
  {
    label: 'Installations',
    value: String(galleryInstallations.length),
    detail: 'Filterable route-linked interface and layout studies.',
  },
  {
    label: 'Art direction',
    value: 'Chicago + cinema',
    detail:
      'A moody, architectural visual system without losing clarity or professionalism.',
  },
  {
    label: 'View modes',
    value: 'Poster / Salon / Index',
    detail: 'Move from visual exploration to fast route review.',
  },
];

const galleryPrinciples = [
  {
    title: 'Built from real scope',
    copy: 'Each study starts with real service and product logic instead of agency filler.',
  },
  {
    title: 'Exploration with boundaries',
    copy: 'The page still experiments, but the route structure stays readable and mobile-friendly.',
  },
  {
    title: 'Tied back to the live site',
    copy: 'Every installation links back to a working route instead of floating as a disconnected mood board.',
  },
];

const galleryRoutes = [
  {
    title: 'Solutions Hub',
    description:
      'Open the main MSP+, security, cloud, AI, and automation entry point.',
    href: withBasePath('services/'),
  },
  {
    title: 'Proof Lab',
    description:
      'See the interactive route that turns visual taste into working buyer proof.',
    href: withBasePath('about/'),
  },
  {
    title: 'Build Studio',
    description:
      'Open the planning route if you want a more interactive system brief.',
    href: withBasePath('build-studio/#studio'),
  },
];

const pageNavigator = {
  label: 'Gallery Guide',
  accent: featuredInstallation.palette[0],
  items: [
    { id: 'gallery-hero', label: 'Gallery Introduction', shortLabel: 'Intro' },
    { id: 'gallery-wall', label: 'Curatorial Wall', shortLabel: 'Wall' },
    {
      id: 'gallery-doctrine',
      label: 'Gallery Doctrine',
      shortLabel: 'Doctrine',
    },
  ],
};
---

<MarketingLayout
  title="Design Gallery | Olive Global Systems"
  description="Filterable design reference library of cinematic interface, layout, and material studies tied back to the live MSP+ site."
  bodyClass="creative-page"
  mainClass="relative isolate overflow-x-clip"
  showFooter={true}
  pageNavigator={pageNavigator}
>
  <StructuredData
    type="collection"
    data={{
      name: 'Design Gallery',
      description:
        'Interactive gallery of cinematic visual studies tied to the live MSP+ site architecture.',
    }}
  />
  <StructuredData
    type="breadcrumbs"
    data={{
      items: [
        {
          '@type': 'ListItem',
          position: 1,
          name: 'Home',
          item: new URL(withBasePath('/'), Astro.site).toString(),
        },
        {
          '@type': 'ListItem',
          position: 2,
          name: 'Design Gallery',
          item: new URL(withBasePath('gallery/'), Astro.site).toString(),
        },
      ],
    }}
  />
  <section
    class="relative scroll-mt-32 overflow-hidden pb-12 pt-20 sm:pb-14 sm:pt-28"
    data-reveal
    id="gallery-hero"
    data-page-section
  >
    <div class="gallery-ambient"></div>
    <div class="mx-auto max-w-[1440px] px-4 sm:px-6">
      <div class="grid gap-8 xl:grid-cols-[1.08fr_0.92fr] xl:items-end">
        <div class="space-y-6">
          <div class="inline-flex flex-wrap gap-2">
            <span class="creative-kicker">Design reference library</span>
            <span class="creative-chip">Filterable wall</span>
            <span class="creative-chip">Route-linked studies</span>
            <span class="creative-chip">Mobile-ready</span>
          </div>

          <div class="space-y-5">
            <h1
              class="tone-title max-w-5xl text-[2.1rem] !leading-[0.96] tracking-normal [text-wrap:balance] min-[400px]:text-4xl min-[500px]:text-5xl sm:text-6xl lg:text-7xl xl:text-[5.45rem]"
            >
              Design references built from
              <span class="creative-gradient-text--accent">
                real systems and client experience work
              </span>
              instead of filler concepts.
            </h1>
            <p class="creative-copy text-white/78 max-w-3xl text-lg sm:text-xl">
              Compare interface language, material direction, and layout systems
              shaped by Chicago architecture, gallery pacing, and cinematic
              restraint—memorable, but still easy to scan.
            </p>
          </div>

          <div class="flex flex-col gap-3 sm:flex-row">
            <ModernButton
              href="#gallery-wall"
              variant="primary"
              className="min-h-[50px]"
            >
              Browse the wall
            </ModernButton>
            <ModernButton
              href={withBasePath('services/')}
              variant="secondary"
              className="min-h-[50px]"
            >
              Open solution hub
            </ModernButton>
            <ModernButton
              href={withBasePath('/')}
              variant="outline"
              className="min-h-[50px]"
            >
              Back to homepage
            </ModernButton>
          </div>

          <div class="grid gap-4 md:grid-cols-3">
            {
              gallerySignals.map(signal => (
                <ModernCard variant="minimal" className="creative-panel p-5">
                  <p class="tone-muted text-xs uppercase tracking-[0.26em]">
                    {signal.label}
                  </p>
                  <p class="tone-title mt-2 text-3xl font-semibold">
                    {signal.value}
                  </p>
                  <p class="tone-body mt-2 text-sm">{signal.detail}</p>
                </ModernCard>
              ))
            }
          </div>
        </div>

        <ModernCard
          variant="minimal"
          className="gallery-feature-card overflow-hidden p-0"
        >
          <div
            class="gallery-feature-stage"
            style={`--gallery-accent:${featuredInstallation.palette[0]};--gallery-secondary:${featuredInstallation.palette[1]};--gallery-shadow:${featuredInstallation.palette[2]};`}
          >
            <div class="gallery-feature-stage__mesh"></div>
            <div class="gallery-feature-stage__orb"></div>
            <div class="gallery-feature-stage__plane"></div>
          </div>
          <div class="space-y-4 p-6">
            <div class="flex flex-wrap items-center gap-2">
              <span class="creative-chip">{featuredInstallation.trade}</span>
              <span class="creative-chip">{featuredInstallation.category}</span>
              <span class="creative-chip">{featuredInstallation.room}</span>
            </div>
            <div>
              <p class="tone-title text-2xl font-semibold">
                {featuredInstallation.title}
              </p>
              <p class="tone-body mt-3 text-sm leading-relaxed">
                {featuredInstallation.summary}
              </p>
            </div>
            <div class="grid gap-3 sm:grid-cols-2">
              {
                featuredInstallation.metrics.map(metric => (
                  <div class="rounded-[1.35rem] border border-white/10 bg-white/[0.03] px-4 py-3">
                    <p class="tone-muted text-[0.68rem] uppercase tracking-[0.24em]">
                      {metric.label}
                    </p>
                    <p class="tone-title mt-2 text-sm font-semibold">
                      {metric.value}
                    </p>
                  </div>
                ))
              }
            </div>
            <ModernButton
              href={featuredInstallation.sourceHref}
              variant="secondary"
              className="min-h-[48px]"
            >
              {featuredInstallation.sourceLabel}
            </ModernButton>
          </div>
        </ModernCard>
      </div>
    </div>
  </section>

  <section
    class="relative z-20 scroll-mt-32"
    data-reveal
    id="gallery-wall"
    data-page-section
  >
    <div class="mx-auto max-w-[1440px] px-4 py-6 sm:px-6 sm:py-10">
      <div class="creative-hero-shell p-5 sm:p-8">
        <div class="flex flex-col gap-6 border-b border-white/10 pb-6">
          <div class="space-y-3">
            <p class="creative-kicker">Browse the wall</p>
            <h2 class="tone-title text-4xl font-semibold tracking-tight">
              Filter by discipline or switch into a faster scan mode.
            </h2>
            <p class="tone-body max-w-3xl text-sm leading-relaxed">
              The controls stay intentionally light so you can explore fast,
              then jump into a linked route without fighting the interface.
            </p>
          </div>

          <div class="grid gap-5 xl:grid-cols-[1fr_auto] xl:items-end">
            <div
              class="flex flex-wrap gap-2"
              aria-label="Filter installations by category"
              data-gallery-filter-group
            >
              <button
                type="button"
                class="gallery-toggle is-active"
                data-gallery-filter="All"
                aria-pressed="true"
              >
                All disciplines
              </button>
              {
                galleryCategories.map(category => (
                  <button
                    type="button"
                    class="gallery-toggle"
                    data-gallery-filter={category}
                    aria-pressed="false"
                  >
                    {category}
                  </button>
                ))
              }
            </div>

            <div
              class="flex flex-wrap gap-2"
              aria-label="Change gallery view"
              data-gallery-view-group
            >
              <button
                type="button"
                class="gallery-toggle is-active"
                data-gallery-view="poster"
                aria-pressed="true"
              >
                Poster
              </button>
              <button
                type="button"
                class="gallery-toggle"
                data-gallery-view="salon"
                aria-pressed="false"
              >
                Salon
              </button>
              <button
                type="button"
                class="gallery-toggle"
                data-gallery-view="index"
                aria-pressed="false"
              >
                Index
              </button>
            </div>
          </div>

          <div class="flex flex-wrap items-center gap-3">
            <span class="creative-chip">Filterable wall</span>
            <span class="creative-chip">Linked working routes</span>
            <span class="creative-chip" data-gallery-active-state>
              Poster view • All disciplines
            </span>
            <p class="tone-muted text-sm" data-gallery-count>
              Showing {galleryInstallations.length} installations
            </p>
          </div>
          <div class="gallery-state-bar">
            <div class="gallery-state-bar__copy">
              <p class="tone-title text-sm font-semibold">Current view</p>
              <p class="tone-body text-sm" data-gallery-state-summary>
                All disciplines in poster view.
              </p>
            </div>
            <div class="gallery-state-bar__actions">
              <button
                type="button"
                class="gallery-state-button"
                data-gallery-copy-state
              >
                Copy state link
              </button>
              <button
                type="button"
                class="gallery-state-button"
                data-gallery-reset-state
              >
                Reset wall
              </button>
            </div>
          </div>
        </div>

        <div class="gallery-grid is-poster mt-8" data-gallery-grid>
          {
            galleryInstallations.map(installation => (
              <article
                class={`gallery-card gallery-card--${installation.size}`}
                data-gallery-card
                data-gallery-category={installation.category}
                style={`--gallery-accent:${installation.palette[0]};--gallery-secondary:${installation.palette[1]};--gallery-shadow:${installation.palette[2]};`}
              >
                <div class="gallery-card__poster">
                  <div class="gallery-card__poster-grid" />
                  <div class="gallery-card__poster-orb" />
                  <div class="gallery-card__poster-ribbon" />
                  <div class="gallery-card__poster-chip">
                    {installation.room}
                  </div>
                </div>

                <div class="gallery-card__content">
                  <div class="flex flex-wrap gap-2">
                    <span class="creative-chip">{installation.trade}</span>
                    <span class="creative-chip">{installation.category}</span>
                    <span class="creative-chip">{installation.medium}</span>
                  </div>

                  <div class="space-y-3">
                    <h3 class="tone-title text-2xl font-semibold">
                      {installation.title}
                    </h3>
                    <p class="tone-body text-sm leading-relaxed">
                      {installation.summary}
                    </p>
                  </div>

                  <div class="grid gap-3 sm:grid-cols-2">
                    {installation.metrics.map(metric => (
                      <div class="gallery-card__metric">
                        <p class="tone-muted text-[0.65rem] uppercase tracking-[0.24em]">
                          {metric.label}
                        </p>
                        <p class="tone-title mt-2 text-sm font-semibold">
                          {metric.value}
                        </p>
                      </div>
                    ))}
                  </div>

                  <details class="gallery-card__notes">
                    <summary>Curator note</summary>
                    <p>{installation.curatorNote}</p>
                  </details>

                  <div class="flex flex-wrap gap-2">
                    {installation.tags.map(tag => (
                      <span class="gallery-tag">{tag}</span>
                    ))}
                  </div>

                  <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
                    <p class="tone-muted text-xs uppercase tracking-[0.24em]">
                      Linked into the live site
                    </p>
                    <ModernButton
                      href={installation.sourceHref}
                      variant="secondary"
                      className="min-h-[46px]"
                    >
                      {installation.sourceLabel}
                    </ModernButton>
                  </div>
                </div>
              </article>
            ))
          }
        </div>
      </div>
    </div>
  </section>

  <section
    class="relative z-20 scroll-mt-32"
    data-reveal
    id="gallery-doctrine"
    data-page-section
  >
    <div class="mx-auto max-w-[1440px] px-4 py-6 sm:px-6 sm:py-10">
      <div class="grid gap-5 xl:grid-cols-[0.98fr_1.02fr]">
        <ModernCard variant="minimal" className="creative-panel p-7">
          <p class="creative-kicker">Why this page exists</p>
          <div class="mt-6 space-y-5">
            {
              galleryPrinciples.map((principle, index) => (
                <div class="flex items-start gap-4">
                  <span class="inline-flex h-9 w-9 items-center justify-center rounded-full border border-white/10 font-mono text-xs text-white/70">
                    0{index + 1}
                  </span>
                  <div>
                    <p class="tone-title text-lg font-semibold">
                      {principle.title}
                    </p>
                    <p class="tone-body mt-2 text-sm leading-relaxed">
                      {principle.copy}
                    </p>
                  </div>
                </div>
              ))
            }
          </div>
        </ModernCard>

        <ModernCard variant="minimal" className="creative-panel p-7">
          <p class="creative-kicker">Where to go next</p>
          <div class="mt-6 grid gap-4">
            {
              galleryRoutes.map(route => (
                <a
                  href={route.href}
                  class="gallery-route-card rounded-[1.6rem] border border-white/10 bg-white/[0.03] p-5 transition hover:border-white/20 hover:bg-white/[0.05]"
                >
                  <p class="tone-title text-xl font-semibold">{route.title}</p>
                  <p class="tone-body mt-2 text-sm">{route.description}</p>
                </a>
              ))
            }
          </div>
        </ModernCard>
      </div>
    </div>
  </section>
</MarketingLayout>

<script is:inline>
  (() => {
    const STORAGE_KEY = 'olive-gallery-state';
    const FILTER_PARAM = 'discipline';
    const VIEW_PARAM = 'galleryView';

    const initGallery = () => {
      const grid = document.querySelector('[data-gallery-grid]');
      const count = document.querySelector('[data-gallery-count]');
      const activeState = document.querySelector('[data-gallery-active-state]');
      const summary = document.querySelector('[data-gallery-state-summary]');
      const copyButton = document.querySelector('[data-gallery-copy-state]');
      const resetButton = document.querySelector('[data-gallery-reset-state]');
      const filterButtons = Array.from(
        document.querySelectorAll('[data-gallery-filter]')
      );
      const viewButtons = Array.from(
        document.querySelectorAll('[data-gallery-view]')
      );
      const cards = Array.from(
        document.querySelectorAll('[data-gallery-card]')
      );

      if (!(grid instanceof HTMLElement) || !(count instanceof HTMLElement)) {
        return;
      }

      if (grid.dataset.galleryBound === 'true') {
        return;
      }

      grid.dataset.galleryBound = 'true';

      const availableFilters = new Set(
        filterButtons
          .map(button =>
            button instanceof HTMLElement ? button.dataset.galleryFilter : ''
          )
          .filter(Boolean)
      );
      const availableViews = new Set(
        viewButtons
          .map(button =>
            button instanceof HTMLElement ? button.dataset.galleryView : ''
          )
          .filter(Boolean)
      );

      const readStoredState = () => {
        try {
          const raw = window.localStorage.getItem(STORAGE_KEY);
          return raw ? JSON.parse(raw) : null;
        } catch {
          return null;
        }
      };

      const getInitialFilter = () => {
        const params = new URLSearchParams(window.location.search);
        const fromUrl = params.get(FILTER_PARAM) || '';
        const fromStorage = readStoredState()?.filter || '';
        const nextFilter = fromUrl || fromStorage || 'All';
        return availableFilters.has(nextFilter) ? nextFilter : 'All';
      };

      const getInitialView = () => {
        const params = new URLSearchParams(window.location.search);
        const fromUrl = params.get(VIEW_PARAM) || '';
        const fromStorage = readStoredState()?.view || '';
        const nextView = fromUrl || fromStorage || 'poster';
        return availableViews.has(nextView) ? nextView : 'poster';
      };

      let activeFilter = getInitialFilter();
      let activeView = getInitialView();

      const syncControlState = () => {
        filterButtons.forEach(node => {
          if (!(node instanceof HTMLButtonElement)) return;
          const isActive = (node.dataset.galleryFilter ?? '') === activeFilter;
          node.classList.toggle('is-active', isActive);
          node.setAttribute('aria-pressed', String(isActive));
        });

        viewButtons.forEach(node => {
          if (!(node instanceof HTMLButtonElement)) return;
          const isActive = (node.dataset.galleryView ?? '') === activeView;
          node.classList.toggle('is-active', isActive);
          node.setAttribute('aria-pressed', String(isActive));
        });
      };

      const syncUrlAndStorage = () => {
        const nextUrl = new URL(window.location.href);
        if (activeFilter === 'All') {
          nextUrl.searchParams.delete(FILTER_PARAM);
        } else {
          nextUrl.searchParams.set(FILTER_PARAM, activeFilter);
        }

        if (activeView === 'poster') {
          nextUrl.searchParams.delete(VIEW_PARAM);
        } else {
          nextUrl.searchParams.set(VIEW_PARAM, activeView);
        }

        window.history.replaceState({}, '', nextUrl.toString());

        try {
          window.localStorage.setItem(
            STORAGE_KEY,
            JSON.stringify({
              filter: activeFilter,
              view: activeView,
            })
          );
        } catch {
          // ignore storage failures
        }
      };

      const render = () => {
        let visibleCount = 0;

        cards.forEach(card => {
          if (!(card instanceof HTMLElement)) return;

          const category = card.dataset.galleryCategory ?? '';
          const isVisible = activeFilter === 'All' || category === activeFilter;

          card.hidden = !isVisible;
          if (isVisible) visibleCount += 1;
        });

        grid.classList.remove('is-poster', 'is-salon', 'is-index');
        grid.classList.add(`is-${activeView}`);
        count.textContent = `Showing ${visibleCount} installation${visibleCount === 1 ? '' : 's'}`;

        const filterLabel =
          activeFilter === 'All' ? 'All disciplines' : activeFilter;
        const viewLabel =
          activeView === 'poster'
            ? 'Poster view'
            : activeView === 'salon'
              ? 'Salon view'
              : 'Index view';

        if (activeState instanceof HTMLElement) {
          activeState.textContent = `${viewLabel} • ${filterLabel}`;
        }

        if (summary instanceof HTMLElement) {
          summary.textContent = `${filterLabel} active in ${viewLabel.toLowerCase()}.`;
        }

        syncControlState();
        syncUrlAndStorage();
      };

      filterButtons.forEach(button => {
        if (!(button instanceof HTMLButtonElement)) return;
        button.addEventListener('click', () => {
          activeFilter = button.dataset.galleryFilter ?? 'All';
          render();
        });
      });

      viewButtons.forEach(button => {
        if (!(button instanceof HTMLButtonElement)) return;
        button.addEventListener('click', () => {
          activeView = button.dataset.galleryView ?? 'poster';
          render();
        });
      });

      if (copyButton instanceof HTMLButtonElement) {
        copyButton.addEventListener('click', async () => {
          try {
            await navigator.clipboard.writeText(window.location.href);
            copyButton.textContent = 'Copied';
          } catch {
            copyButton.textContent = 'Unavailable';
          }

          window.setTimeout(() => {
            copyButton.textContent = 'Copy state link';
          }, 1200);
        });
      }

      if (resetButton instanceof HTMLButtonElement) {
        resetButton.addEventListener('click', () => {
          activeFilter = 'All';
          activeView = 'poster';
          render();
        });
      }

      render();
    };

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initGallery, {
        once: true,
      });
    } else {
      initGallery();
    }

    document.addEventListener('astro:page-load', initGallery);
  })();
</script>

<style is:inline>
  .gallery-ambient {
    position: absolute;
    inset: 0;
    pointer-events: none;
    background:
      radial-gradient(
        circle at 15% 10%,
        rgba(125, 211, 252, 0.08),
        transparent 26%
      ),
      radial-gradient(
        circle at 88% 14%,
        rgba(148, 163, 184, 0.1),
        transparent 24%
      ),
      linear-gradient(180deg, rgba(2, 4, 10, 0), rgba(2, 4, 10, 0.84));
  }

  .gallery-feature-card {
    background:
      linear-gradient(
        155deg,
        rgba(255, 255, 255, 0.08),
        rgba(255, 255, 255, 0.02)
      ),
      rgba(4, 6, 12, 0.86);
  }

  .gallery-feature-stage {
    position: relative;
    min-height: 280px;
    overflow: hidden;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    background:
      radial-gradient(
        circle at 24% 18%,
        color-mix(in srgb, var(--gallery-accent) 46%, transparent),
        transparent 26%
      ),
      radial-gradient(
        circle at 78% 26%,
        color-mix(in srgb, var(--gallery-secondary) 42%, transparent),
        transparent 28%
      ),
      linear-gradient(
        135deg,
        color-mix(in srgb, var(--gallery-shadow) 82%, black 18%),
        rgba(9, 14, 22, 0.96)
      );
  }

  .gallery-feature-stage__mesh,
  .gallery-feature-stage__orb,
  .gallery-feature-stage__plane {
    position: absolute;
    pointer-events: none;
  }

  .gallery-feature-stage__mesh {
    inset: 10% 8%;
    border-radius: 2rem;
    border: 1px solid rgba(255, 255, 255, 0.1);
    background-image:
      linear-gradient(rgba(255, 255, 255, 0.08) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.08) 1px, transparent 1px);
    background-size: 34px 34px;
    mask-image: linear-gradient(180deg, rgba(0, 0, 0, 0.82), transparent);
  }

  .gallery-feature-stage__orb {
    top: 18%;
    right: 14%;
    height: 180px;
    width: 180px;
    border-radius: 999px;
    background: radial-gradient(
      circle at 30% 30%,
      rgba(255, 255, 255, 0.95),
      color-mix(in srgb, var(--gallery-accent) 50%, white 50%) 26%,
      transparent 72%
    );
    filter: blur(2px);
    opacity: 0.88;
  }

  .gallery-feature-stage__plane {
    inset: auto 10% 14% 14%;
    height: 92px;
    border-radius: 999px;
    background: linear-gradient(
      90deg,
      color-mix(in srgb, var(--gallery-accent) 0%, transparent),
      color-mix(in srgb, var(--gallery-secondary) 46%, transparent),
      color-mix(in srgb, var(--gallery-accent) 18%, transparent)
    );
    filter: blur(28px);
    opacity: 0.9;
  }

  .gallery-toggle {
    display: inline-flex;
    min-height: 44px;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    background: rgba(255, 255, 255, 0.03);
    padding: 0.72rem 1rem;
    font-family: var(--font-mono);
    font-size: 0.7rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.68);
    transition:
      transform 0.25s ease,
      border-color 0.25s ease,
      background 0.25s ease,
      color 0.25s ease;
  }

  @media (hover: hover) {
    .gallery-toggle:hover {
      transform: translateY(-1px);
      border-color: rgba(255, 255, 255, 0.18);
      color: rgba(255, 255, 255, 0.92);
    }
  }

  .gallery-toggle:active {
    transform: scale(0.96);
  }

  .gallery-toggle.is-active {
    border-color: rgba(148, 163, 184, 0.34);
    background: linear-gradient(
      135deg,
      rgba(125, 211, 252, 0.12),
      rgba(148, 163, 184, 0.08)
    );
    color: white;
    box-shadow: 0 0 30px -18px rgba(125, 211, 252, 0.28);
  }

  .gallery-grid {
    display: grid;
    gap: 1.25rem;
  }

  .gallery-grid.is-poster {
    grid-template-columns: repeat(1, minmax(0, 1fr));
  }

  .gallery-grid.is-salon {
    grid-template-columns: repeat(1, minmax(0, 1fr));
  }

  .gallery-grid.is-index {
    grid-template-columns: minmax(0, 1fr);
  }

  .gallery-card {
    position: relative;
    display: grid;
    gap: 0;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 1.35rem;
    background:
      linear-gradient(
        160deg,
        rgba(255, 255, 255, 0.06),
        rgba(255, 255, 255, 0)
      ),
      rgba(5, 7, 14, 0.88);
    box-shadow: 0 20px 54px -32px rgba(0, 0, 0, 0.88);
    transition:
      transform 0.35s cubic-bezier(0.16, 1, 0.3, 1),
      border-color 0.35s ease,
      box-shadow 0.35s ease;
    -webkit-tap-highlight-color: transparent;
    touch-action: manipulation;
  }

  @media (hover: hover) {
    .gallery-card:hover {
      transform: translateY(-2px);
      border-color: color-mix(in srgb, var(--gallery-accent) 32%, transparent);
      box-shadow:
        0 28px 68px -36px rgba(0, 0, 0, 0.9),
        0 0 42px -24px
          color-mix(in srgb, var(--gallery-accent) 36%, transparent);
    }
  }

  .gallery-card:active {
    transform: scale(0.985);
  }

  .gallery-card__poster {
    position: relative;
    min-height: 260px;
    overflow: hidden;
    background:
      radial-gradient(
        circle at 18% 16%,
        color-mix(in srgb, var(--gallery-accent) 46%, transparent),
        transparent 26%
      ),
      radial-gradient(
        circle at 86% 22%,
        color-mix(in srgb, var(--gallery-secondary) 44%, transparent),
        transparent 28%
      ),
      linear-gradient(
        145deg,
        color-mix(in srgb, var(--gallery-shadow) 88%, black 12%),
        rgba(8, 10, 18, 0.96)
      );
  }

  .gallery-card__poster-grid,
  .gallery-card__poster-orb,
  .gallery-card__poster-ribbon,
  .gallery-card__poster-chip {
    position: absolute;
  }

  .gallery-card__poster-grid {
    inset: 8%;
    border-radius: 1.5rem;
    border: 1px solid rgba(255, 255, 255, 0.08);
    background-image:
      linear-gradient(rgba(255, 255, 255, 0.08) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.08) 1px, transparent 1px);
    background-size: 22px 22px;
    mask-image: linear-gradient(180deg, rgba(0, 0, 0, 1), transparent 92%);
  }

  .gallery-card__poster-orb {
    top: 16%;
    right: 12%;
    height: 144px;
    width: 144px;
    border-radius: 999px;
    background: radial-gradient(
      circle at 36% 28%,
      rgba(255, 255, 255, 0.98),
      color-mix(in srgb, var(--gallery-accent) 42%, white 58%) 28%,
      transparent 70%
    );
    filter: blur(1px);
    opacity: 0.86;
  }

  .gallery-card__poster-ribbon {
    left: 8%;
    right: 22%;
    bottom: 14%;
    height: 68px;
    border-radius: 999px;
    background: linear-gradient(
      90deg,
      color-mix(in srgb, var(--gallery-accent) 0%, transparent),
      color-mix(in srgb, var(--gallery-secondary) 50%, transparent),
      color-mix(in srgb, var(--gallery-accent) 20%, transparent)
    );
    filter: blur(24px);
    opacity: 0.88;
  }

  .gallery-card__poster-chip {
    left: 1rem;
    top: 1rem;
    border-radius: 999px;
    border: 1px solid rgba(255, 255, 255, 0.12);
    background: rgba(5, 7, 14, 0.35);
    padding: 0.5rem 0.8rem;
    font-family: var(--font-mono);
    font-size: 0.62rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.72);
    backdrop-filter: blur(14px);
  }

  .gallery-card__content {
    display: grid;
    gap: 1.1rem;
    padding: 1.15rem;
  }

  .gallery-card__metric {
    border-radius: 1.15rem;
    border: 1px solid rgba(255, 255, 255, 0.09);
    background: rgba(255, 255, 255, 0.03);
    padding: 0.85rem 0.95rem;
  }

  .gallery-card__notes {
    border-radius: 1.15rem;
    border: 1px solid rgba(255, 255, 255, 0.09);
    background: rgba(255, 255, 255, 0.03);
    padding: 0.95rem 1rem;
  }

  .gallery-card__notes summary {
    cursor: pointer;
    list-style: none;
    font-family: var(--font-mono);
    font-size: 0.7rem;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.78);
  }

  .gallery-card__notes summary::-webkit-details-marker {
    display: none;
  }

  .gallery-card__notes p {
    margin: 0.85rem 0 0;
    color: rgba(255, 255, 255, 0.68);
    font-size: 0.92rem;
    line-height: 1.7;
  }

  .gallery-tag {
    display: inline-flex;
    align-items: center;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.05);
    padding: 0.45rem 0.78rem;
    font-size: 0.72rem;
    color: rgba(255, 255, 255, 0.72);
  }

  .gallery-route-card {
    position: relative;
    overflow: hidden;
  }

  .gallery-route-card::after {
    content: '';
    position: absolute;
    inset: auto 14% -40% 14%;
    height: 120px;
    border-radius: 999px;
    background: radial-gradient(
      circle,
      rgba(125, 211, 252, 0.1),
      transparent 72%
    );
    filter: blur(22px);
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  @media (hover: hover) {
    .gallery-route-card:hover::after {
      opacity: 1;
    }
  }

  .gallery-route-card {
    -webkit-tap-highlight-color: transparent;
    touch-action: manipulation;
  }

  .gallery-route-card:active {
    background: rgba(255, 255, 255, 0.05);
  }

  .gallery-state-bar {
    display: grid;
    gap: 0.9rem;
    margin-top: 1rem;
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 1.35rem;
    background:
      linear-gradient(
        160deg,
        rgba(255, 255, 255, 0.05),
        rgba(255, 255, 255, 0.02)
      ),
      rgba(5, 7, 14, 0.72);
    padding: 1rem 1.05rem;
  }

  .gallery-state-bar__copy {
    display: grid;
    gap: 0.2rem;
  }

  .gallery-state-bar__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
  }

  .gallery-state-button {
    display: inline-flex;
    min-height: 44px;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    background: rgba(255, 255, 255, 0.03);
    padding: 0.72rem 1rem;
    font-family: var(--font-mono);
    font-size: 0.68rem;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.72);
    transition:
      transform 0.25s ease,
      border-color 0.25s ease,
      background-color 0.25s ease,
      color 0.25s ease;
  }

  @media (hover: hover) {
    .gallery-state-button:hover {
      transform: translateY(-1px);
      border-color: rgba(255, 255, 255, 0.18);
      background: rgba(255, 255, 255, 0.06);
      color: rgba(255, 255, 255, 0.94);
    }
  }

  .gallery-state-button:active {
    transform: scale(0.96);
    background: rgba(255, 255, 255, 0.06);
  }

  @media (min-width: 900px) {
    .gallery-state-bar {
      grid-template-columns: minmax(0, 1fr) auto;
      align-items: center;
    }

    .gallery-grid.is-poster {
      grid-template-columns: repeat(12, minmax(0, 1fr));
    }

    .gallery-grid.is-salon {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .gallery-card--feature {
      grid-column: span 7;
    }

    .gallery-card--wide {
      grid-column: span 7;
    }

    .gallery-card--tall {
      grid-column: span 5;
    }

    .gallery-card--standard {
      grid-column: span 5;
    }

    .gallery-grid.is-poster .gallery-card--feature .gallery-card__poster,
    .gallery-grid.is-poster .gallery-card--wide .gallery-card__poster {
      min-height: 320px;
    }

    .gallery-grid.is-poster .gallery-card--tall .gallery-card__poster {
      min-height: 400px;
    }
  }

  @media (min-width: 1100px) {
    .gallery-grid.is-poster .gallery-card {
      grid-template-columns: minmax(280px, 0.9fr) minmax(0, 1.1fr);
    }

    .gallery-grid.is-poster .gallery-card--tall {
      grid-template-columns: minmax(250px, 0.84fr) minmax(0, 1.16fr);
    }
  }

  .gallery-grid.is-salon .gallery-card,
  .gallery-grid.is-index .gallery-card {
    grid-template-columns: minmax(0, 1fr);
  }

  .gallery-grid.is-salon .gallery-card__poster {
    min-height: 240px;
  }

  .gallery-grid.is-index .gallery-card {
    border-radius: 1.4rem;
  }

  .gallery-grid.is-index .gallery-card__poster {
    min-height: 180px;
  }

  .gallery-grid.is-index .gallery-card__content {
    gap: 0.9rem;
  }

  /* Mobile grid: 2-col for index/salon on wider phones */
  @media (min-width: 480px) and (max-width: 899px) {
    .gallery-grid.is-salon {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .gallery-grid.is-index {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .gallery-grid.is-salon .gallery-card__poster {
      min-height: 180px;
    }

    .gallery-grid.is-index .gallery-card__poster {
      min-height: 140px;
    }
  }

  /* Tighten card content padding on small screens */
  @media (max-width: 480px) {
    .gallery-card__content {
      padding: 0.95rem;
      gap: 0.8rem;
    }

    .gallery-card__poster {
      min-height: 180px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .gallery-card {
      transition: none;
    }
  }
</style>
