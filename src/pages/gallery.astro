---
import MarketingLayout from '../layouts/MarketingLayout.astro';
import StructuredData from '../components/StructuredData.astro';
import ModernButton from '../components/ui/ModernButton.astro';
import {
  galleryCategories,
  galleryInstallations,
} from '../data/design-gallery';
import { withBasePath } from '../utils/helpers';

const featuredInstallation = galleryInstallations[0];

const gallerySignals = [
  {
    label: 'Installations',
    value: String(galleryInstallations.length),
    detail: 'Filterable route-linked studies.',
  },
  {
    label: 'View modes',
    value: '3',
    detail: 'Poster, salon, and index.',
  },
  {
    label: 'Purpose',
    value: 'Real route reference',
    detail: 'Not detached mood boards.',
  },
];

const galleryPrinciples = [
  {
    title: 'Built from real scope',
    copy: 'Each study starts from service, route, or buyer-facing system logic instead of filler concepts.',
  },
  {
    title: 'Exploration with discipline',
    copy: 'The work can still be expressive without becoming confusing on a phone.',
  },
  {
    title: 'Connected back to the site',
    copy: 'Every installation points to a real route so the gallery stays useful.',
  },
];

const galleryRoutes = [
  {
    title: 'Solutions Hub',
    description:
      'Open the main managed IT, security, Microsoft 365, cloud, and workflow entry point.',
    href: withBasePath('services/'),
  },
  {
    title: 'E Commerce Demo',
    description:
      'See the interactive route that turns visual taste into working buyer proof.',
    href: withBasePath('about/'),
  },
  {
    title: 'Contact HQ',
    description:
      'Route a concrete brief when the gallery direction needs to become scoped work.',
    href: withBasePath('contact-hq/'),
  },
];
---

<MarketingLayout
  title="Design Gallery | Olive Global Systems"
  description="Filterable design reference library of cinematic interface, layout, and material studies tied back to the live managed IT site."
  bodyClass="creative-page"
  mainClass="relative isolate overflow-x-clip"
  showFooter={true}
  showStickyCTA={false}
  showSavedRoutesDock={false}
>
  <StructuredData
    type="collection"
    data={{
      name: 'Design Gallery',
      description:
        'Interactive gallery of visual studies tied to the live managed IT site architecture.',
    }}
  />
  <StructuredData
    type="breadcrumbs"
    data={{
      items: [
        {
          '@type': 'ListItem',
          position: 1,
          name: 'Home',
          item: new URL(withBasePath('/'), Astro.site).toString(),
        },
        {
          '@type': 'ListItem',
          position: 2,
          name: 'Design Gallery',
          item: new URL(withBasePath('gallery/'), Astro.site).toString(),
        },
      ],
    }}
  />

  <section class="gallery-page-hero" id="gallery-hero" data-page-section>
    <div class="premium-container">
      <div class="gallery-page-hero__shell">
        <div class="gallery-page-hero__copy">
          <p class="gallery-page-eyebrow">Design gallery</p>
          <h1 class="gallery-page-title">
            Visual references built from
            <span>real routes, systems, and</span>
            <span>client-facing work.</span>
          </h1>
          <p class="gallery-page-copy">
            This page is for reviewing direction, composition, and interface
            tone without getting buried in decorative noise or disconnected
            concept art.
          </p>

          <div class="gallery-page-actions">
            <ModernButton
              href="#gallery-wall"
              variant="primary"
              className="min-h-[52px]"
            >
              Browse the wall
            </ModernButton>
            <ModernButton
              href={withBasePath('services/')}
              variant="outline"
              className="min-h-[52px]"
            >
              Open solutions
            </ModernButton>
          </div>
        </div>

        <div class="gallery-page-feature">
          <div
            class="gallery-page-feature__art"
            style={`--gallery-accent:${featuredInstallation.palette[0]};--gallery-secondary:${featuredInstallation.palette[1]};--gallery-shadow:${featuredInstallation.palette[2]};`}
          >
            <span>{featuredInstallation.room}</span>
          </div>
          <div class="gallery-page-feature__body">
            <p class="gallery-page-feature__label">Featured study</p>
            <h2>{featuredInstallation.title}</h2>
            <p>{featuredInstallation.summary}</p>
            <div class="gallery-page-feature__meta">
              <span>{featuredInstallation.trade}</span>
              <span>{featuredInstallation.category}</span>
              <span>{featuredInstallation.medium}</span>
            </div>
          </div>
        </div>

        <div class="gallery-page-signal-list">
          {
            gallerySignals.map(signal => (
              <div class="gallery-page-signal">
                <p class="gallery-page-signal__label">{signal.label}</p>
                <p class="gallery-page-signal__value">{signal.value}</p>
                <p class="gallery-page-signal__detail">{signal.detail}</p>
              </div>
            ))
          }
        </div>
      </div>
    </div>
  </section>

  <section class="gallery-page-section" id="gallery-wall" data-page-section>
    <div class="premium-container">
      <div class="gallery-page-section__intro">
        <p class="gallery-page-eyebrow">Browse the wall</p>
        <h2 class="gallery-page-section__title">
          Filter by discipline or change the scan mode.
        </h2>
        <p class="gallery-page-section__copy">
          The controls stay light so you can explore fast and then jump into a
          linked working route.
        </p>
      </div>

      <div class="gallery-controls">
        <div
          class="gallery-controls__group"
          aria-label="Filter installations by category"
          data-gallery-filter-group
        >
          <button
            type="button"
            class="gallery-toggle is-active"
            data-gallery-filter="All"
            aria-pressed="true"
          >
            All disciplines
          </button>
          {
            galleryCategories.map(category => (
              <button
                type="button"
                class="gallery-toggle"
                data-gallery-filter={category}
                aria-pressed="false"
              >
                {category}
              </button>
            ))
          }
        </div>

        <div
          class="gallery-controls__group"
          aria-label="Change gallery view"
          data-gallery-view-group
        >
          <button
            type="button"
            class="gallery-toggle is-active"
            data-gallery-view="poster"
            aria-pressed="true"
          >
            Poster
          </button>
          <button
            type="button"
            class="gallery-toggle"
            data-gallery-view="salon"
            aria-pressed="false"
          >
            Salon
          </button>
          <button
            type="button"
            class="gallery-toggle"
            data-gallery-view="index"
            aria-pressed="false"
          >
            Index
          </button>
        </div>
      </div>

      <div class="gallery-state-bar">
        <div>
          <p class="gallery-state-bar__label">Current wall state</p>
          <p class="gallery-state-bar__value" data-gallery-active-state>
            Poster view • All disciplines
          </p>
          <p class="gallery-state-bar__copy" data-gallery-state-summary>
            All disciplines in poster view.
          </p>
        </div>

        <div class="gallery-state-bar__actions">
          <p class="gallery-state-bar__count" data-gallery-count>
            Showing {galleryInstallations.length} installations
          </p>
          <button
            type="button"
            class="gallery-state-button"
            data-gallery-copy-state
          >
            Copy state link
          </button>
          <button
            type="button"
            class="gallery-state-button"
            data-gallery-reset-state
          >
            Reset wall
          </button>
        </div>
      </div>

      <div class="gallery-grid is-poster" data-gallery-grid>
        {
          galleryInstallations.map(installation => (
            <article
              class={`gallery-card gallery-card--${installation.size}`}
              data-gallery-card
              data-gallery-category={installation.category}
              style={`--gallery-accent:${installation.palette[0]};--gallery-secondary:${installation.palette[1]};--gallery-shadow:${installation.palette[2]};`}
            >
              <div class="gallery-card__art">
                <div class="gallery-card__art-grid" />
                <div class="gallery-card__art-mark">{installation.room}</div>
              </div>

              <div class="gallery-card__body">
                <div class="gallery-card__meta">
                  <span>{installation.trade}</span>
                  <span>{installation.category}</span>
                  <span>{installation.medium}</span>
                </div>

                <div class="gallery-card__copy">
                  <h3>{installation.title}</h3>
                  <p>{installation.summary}</p>
                </div>

                <div class="gallery-card__metrics">
                  {installation.metrics.map(metric => (
                    <div class="gallery-card__metric">
                      <p>{metric.label}</p>
                      <strong>{metric.value}</strong>
                    </div>
                  ))}
                </div>

                <details class="gallery-card__notes">
                  <summary>Curator note</summary>
                  <p>{installation.curatorNote}</p>
                </details>

                <div class="gallery-card__tags">
                  {installation.tags.map(tag => (
                    <span>{tag}</span>
                  ))}
                </div>

                <div class="gallery-card__footer">
                  <p>Linked into the live site</p>
                  <ModernButton
                    href={installation.sourceHref}
                    variant="outline"
                    className="min-h-[46px]"
                  >
                    {installation.sourceLabel}
                  </ModernButton>
                </div>
              </div>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <section class="gallery-page-section" id="gallery-doctrine" data-page-section>
    <div class="premium-container">
      <div class="gallery-doctrine-grid">
        <div class="gallery-doctrine-block">
          <p class="gallery-page-eyebrow">Why this page exists</p>
          <div class="gallery-doctrine-list">
            {
              galleryPrinciples.map((principle, index) => (
                <div class="gallery-doctrine-item">
                  <span>0{index + 1}</span>
                  <div>
                    <h3>{principle.title}</h3>
                    <p>{principle.copy}</p>
                  </div>
                </div>
              ))
            }
          </div>
        </div>

        <div class="gallery-doctrine-block">
          <p class="gallery-page-eyebrow">Where to go next</p>
          <div class="gallery-route-list">
            {
              galleryRoutes.map(route => (
                <a href={route.href} class="gallery-route-card">
                  <h3>{route.title}</h3>
                  <p>{route.description}</p>
                </a>
              ))
            }
          </div>
        </div>
      </div>
    </div>
  </section>
</MarketingLayout>

<script is:inline>
  (() => {
    const STORAGE_KEY = 'olive-gallery-state';
    const FILTER_PARAM = 'discipline';
    const VIEW_PARAM = 'galleryView';

    const initGallery = () => {
      const grid = document.querySelector('[data-gallery-grid]');
      const count = document.querySelector('[data-gallery-count]');
      const activeState = document.querySelector('[data-gallery-active-state]');
      const summary = document.querySelector('[data-gallery-state-summary]');
      const copyButton = document.querySelector('[data-gallery-copy-state]');
      const resetButton = document.querySelector('[data-gallery-reset-state]');
      const filterButtons = Array.from(
        document.querySelectorAll('[data-gallery-filter]')
      );
      const viewButtons = Array.from(
        document.querySelectorAll('[data-gallery-view]')
      );
      const cards = Array.from(
        document.querySelectorAll('[data-gallery-card]')
      );

      if (!(grid instanceof HTMLElement) || !(count instanceof HTMLElement)) {
        return;
      }

      if (grid.dataset.galleryBound === 'true') {
        return;
      }

      grid.dataset.galleryBound = 'true';

      const availableFilters = new Set(
        filterButtons
          .map(button =>
            button instanceof HTMLElement ? button.dataset.galleryFilter : ''
          )
          .filter(Boolean)
      );
      const availableViews = new Set(
        viewButtons
          .map(button =>
            button instanceof HTMLElement ? button.dataset.galleryView : ''
          )
          .filter(Boolean)
      );

      const readStoredState = () => {
        try {
          const raw = window.localStorage.getItem(STORAGE_KEY);
          return raw ? JSON.parse(raw) : null;
        } catch {
          return null;
        }
      };

      const getInitialFilter = () => {
        const params = new URLSearchParams(window.location.search);
        const fromUrl = params.get(FILTER_PARAM) || '';
        const fromStorage = readStoredState()?.filter || '';
        const nextFilter = fromUrl || fromStorage || 'All';
        return availableFilters.has(nextFilter) ? nextFilter : 'All';
      };

      const getInitialView = () => {
        const params = new URLSearchParams(window.location.search);
        const fromUrl = params.get(VIEW_PARAM) || '';
        const fromStorage = readStoredState()?.view || '';
        const nextView = fromUrl || fromStorage || 'poster';
        return availableViews.has(nextView) ? nextView : 'poster';
      };

      let activeFilter = getInitialFilter();
      let activeView = getInitialView();

      const syncControlState = () => {
        filterButtons.forEach(node => {
          if (!(node instanceof HTMLButtonElement)) return;
          const isActive = (node.dataset.galleryFilter ?? '') === activeFilter;
          node.classList.toggle('is-active', isActive);
          node.setAttribute('aria-pressed', String(isActive));
        });

        viewButtons.forEach(node => {
          if (!(node instanceof HTMLButtonElement)) return;
          const isActive = (node.dataset.galleryView ?? '') === activeView;
          node.classList.toggle('is-active', isActive);
          node.setAttribute('aria-pressed', String(isActive));
        });
      };

      const syncUrlAndStorage = () => {
        const nextUrl = new URL(window.location.href);
        if (activeFilter === 'All') {
          nextUrl.searchParams.delete(FILTER_PARAM);
        } else {
          nextUrl.searchParams.set(FILTER_PARAM, activeFilter);
        }

        if (activeView === 'poster') {
          nextUrl.searchParams.delete(VIEW_PARAM);
        } else {
          nextUrl.searchParams.set(VIEW_PARAM, activeView);
        }

        window.history.replaceState({}, '', nextUrl.toString());

        try {
          window.localStorage.setItem(
            STORAGE_KEY,
            JSON.stringify({
              filter: activeFilter,
              view: activeView,
            })
          );
        } catch {
          // ignore storage failures
        }
      };

      const render = () => {
        let visibleCount = 0;

        cards.forEach(card => {
          if (!(card instanceof HTMLElement)) return;

          const category = card.dataset.galleryCategory ?? '';
          const isVisible = activeFilter === 'All' || category === activeFilter;

          card.hidden = !isVisible;
          if (isVisible) visibleCount += 1;
        });

        grid.classList.remove('is-poster', 'is-salon', 'is-index');
        grid.classList.add(`is-${activeView}`);
        count.textContent = `Showing ${visibleCount} installation${visibleCount === 1 ? '' : 's'}`;

        const filterLabel =
          activeFilter === 'All' ? 'All disciplines' : activeFilter;
        const viewLabel =
          activeView === 'poster'
            ? 'Poster view'
            : activeView === 'salon'
              ? 'Salon view'
              : 'Index view';

        if (activeState instanceof HTMLElement) {
          activeState.textContent = `${viewLabel} • ${filterLabel}`;
        }

        if (summary instanceof HTMLElement) {
          summary.textContent = `${filterLabel} active in ${viewLabel.toLowerCase()}.`;
        }

        syncControlState();
        syncUrlAndStorage();
      };

      filterButtons.forEach(button => {
        if (!(button instanceof HTMLButtonElement)) return;
        button.addEventListener('click', () => {
          activeFilter = button.dataset.galleryFilter ?? 'All';
          render();
        });
      });

      viewButtons.forEach(button => {
        if (!(button instanceof HTMLButtonElement)) return;
        button.addEventListener('click', () => {
          activeView = button.dataset.galleryView ?? 'poster';
          render();
        });
      });

      if (copyButton instanceof HTMLButtonElement) {
        copyButton.addEventListener('click', async () => {
          try {
            await navigator.clipboard.writeText(window.location.href);
            copyButton.textContent = 'Copied';
          } catch {
            copyButton.textContent = 'Unavailable';
          }

          window.setTimeout(() => {
            copyButton.textContent = 'Copy state link';
          }, 1200);
        });
      }

      if (resetButton instanceof HTMLButtonElement) {
        resetButton.addEventListener('click', () => {
          activeFilter = 'All';
          activeView = 'poster';
          render();
        });
      }

      render();
    };

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initGallery, {
        once: true,
      });
    } else {
      initGallery();
    }

    document.addEventListener('astro:page-load', initGallery);
  })();
</script>

<style is:inline>
  .gallery-page-hero,
  .gallery-page-section {
    position: relative;
    padding: clamp(4rem, 8vw, 6.5rem) 0;
    border-bottom: 1px solid
      color-mix(in srgb, var(--color-border) 70%, transparent);
  }

  .gallery-page-hero {
    padding-top: clamp(6rem, 9vw, 7.75rem);
  }

  .gallery-page-hero__shell,
  .gallery-doctrine-grid {
    display: grid;
    gap: 2rem;
  }

  .gallery-page-hero__copy,
  .gallery-page-section__intro {
    display: grid;
    gap: 0.9rem;
    max-width: 44rem;
  }

  .gallery-page-eyebrow,
  .gallery-page-signal__label,
  .gallery-state-bar__label,
  .gallery-page-feature__label,
  .gallery-card__metric p,
  .gallery-scenario-label,
  .gallery-card__footer p {
    margin: 0;
    font-family: var(--font-mono);
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: color-mix(in srgb, var(--color-text-muted) 90%, transparent);
  }

  .gallery-page-title,
  .gallery-page-section__title,
  .gallery-page-feature h2,
  .gallery-card h3,
  .gallery-doctrine-item h3,
  .gallery-route-card h3,
  .gallery-state-bar__value {
    margin: 0;
    color: var(--color-text-primary);
    font-family: var(--font-display);
    font-size: clamp(1.4rem, 3vw, 3rem);
    font-weight: 600;
    line-height: 1.02;
    letter-spacing: 0;
    text-wrap: balance;
  }

  .gallery-page-title {
    max-width: 17ch;
    font-size: clamp(2.1rem, 4.8vw, 4.05rem);
    line-height: 1.01;
    letter-spacing: 0;
  }

  .gallery-page-title span {
    display: block;
  }

  .gallery-page-title span:nth-child(1) {
    color: color-mix(in srgb, var(--color-text-primary) 72%, transparent);
  }

  .gallery-page-title span:last-child {
    color: color-mix(in srgb, var(--color-primary) 80%, white 20%);
  }

  .gallery-page-copy,
  .gallery-page-section__copy,
  .gallery-page-feature p,
  .gallery-page-signal__detail,
  .gallery-state-bar__copy,
  .gallery-card__copy p,
  .gallery-card__notes p,
  .gallery-doctrine-item p,
  .gallery-route-card p {
    margin: 0;
    color: color-mix(in srgb, var(--color-text-secondary) 94%, transparent);
    font-size: clamp(1rem, 1.35vw, 1.06rem);
    line-height: 1.7;
  }

  .gallery-page-actions,
  .gallery-controls,
  .gallery-controls__group,
  .gallery-state-bar__actions,
  .gallery-card__meta,
  .gallery-card__tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
  }

  .gallery-page-feature,
  .gallery-page-signal-list,
  .gallery-controls,
  .gallery-state-bar,
  .gallery-doctrine-block {
    border-top: 1px solid
      color-mix(in srgb, var(--color-border) 68%, transparent);
    padding-top: 1.25rem;
  }

  .gallery-page-feature {
    display: grid;
    gap: 1rem;
  }

  .gallery-page-feature__art {
    position: relative;
    min-height: 220px;
    overflow: hidden;
    border-radius: 1rem;
    border: 1px solid color-mix(in srgb, var(--color-border) 70%, transparent);
    background:
      radial-gradient(
        circle at 18% 20%,
        color-mix(in srgb, var(--gallery-accent) 40%, transparent),
        transparent 28%
      ),
      radial-gradient(
        circle at 82% 26%,
        color-mix(in srgb, var(--gallery-secondary) 36%, transparent),
        transparent 28%
      ),
      linear-gradient(
        145deg,
        color-mix(in srgb, var(--gallery-shadow) 84%, black 16%),
        rgba(12, 15, 19, 0.96)
      );
  }

  .gallery-page-feature__art::before {
    content: '';
    position: absolute;
    inset: 1rem;
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 0.85rem;
    background-image:
      linear-gradient(rgba(255, 255, 255, 0.05) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.05) 1px, transparent 1px);
    background-size: 26px 26px;
  }

  .gallery-page-feature__art span {
    position: absolute;
    left: 1rem;
    top: 1rem;
    z-index: 1;
    display: inline-flex;
    min-height: 2rem;
    align-items: center;
    padding: 0 0.8rem;
    border-radius: 999px;
    border: 1px solid rgba(255, 255, 255, 0.12);
    background: rgba(12, 15, 19, 0.42);
    color: rgba(255, 255, 255, 0.74);
    font-family: var(--font-mono);
    font-size: 0.62rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }

  .gallery-page-feature__body {
    display: grid;
    gap: 0.6rem;
  }

  .gallery-page-feature__meta span,
  .gallery-card__meta span,
  .gallery-card__tags span {
    display: inline-flex;
    min-height: 2rem;
    align-items: center;
    padding: 0 0.7rem;
    border-radius: 999px;
    border: 1px solid color-mix(in srgb, var(--color-border) 72%, transparent);
    color: color-mix(in srgb, var(--color-text-primary) 76%, transparent);
    font-size: 0.78rem;
  }

  .gallery-page-signal-list {
    display: grid;
    gap: 1rem;
  }

  .gallery-page-signal {
    padding-bottom: 1rem;
    border-bottom: 1px solid
      color-mix(in srgb, var(--color-border) 64%, transparent);
  }

  .gallery-page-signal__value {
    margin: 0.35rem 0 0;
    color: var(--color-text-primary);
    font-size: 1.3rem;
    font-weight: 600;
    letter-spacing: 0;
  }

  .gallery-toggle,
  .gallery-state-button {
    display: inline-flex;
    min-height: 44px;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    border: 1px solid color-mix(in srgb, var(--color-border) 74%, transparent);
    background: color-mix(in srgb, var(--color-surface) 84%, transparent);
    padding: 0.72rem 1rem;
    color: color-mix(in srgb, var(--color-text-primary) 78%, transparent);
    font-family: var(--font-mono);
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    transition:
      border-color 0.2s ease,
      background-color 0.2s ease,
      transform 0.2s ease;
  }

  .gallery-toggle.is-active {
    border-color: color-mix(in srgb, var(--color-primary) 26%, transparent);
    background: color-mix(in srgb, var(--color-primary) 10%, transparent);
    color: var(--color-text-primary);
  }

  .gallery-toggle:hover,
  .gallery-state-button:hover,
  .gallery-route-card:hover {
    transform: translateX(3px);
    border-color: color-mix(
      in srgb,
      var(--color-text-primary) 18%,
      transparent
    );
  }

  .gallery-state-bar {
    display: grid;
    gap: 1rem;
  }

  .gallery-state-bar__count {
    margin: 0;
    color: color-mix(in srgb, var(--color-text-secondary) 88%, transparent);
    font-size: 0.92rem;
  }

  .gallery-grid {
    display: grid;
    gap: 1rem;
    margin-top: 1.5rem;
  }

  .gallery-grid.is-poster,
  .gallery-grid.is-salon,
  .gallery-grid.is-index {
    grid-template-columns: 1fr;
  }

  .gallery-card {
    display: grid;
    overflow: hidden;
    border: 1px solid color-mix(in srgb, var(--color-border) 72%, transparent);
    border-radius: 1rem;
    background: color-mix(in srgb, var(--color-surface) 84%, transparent);
  }

  .gallery-card__art {
    position: relative;
    min-height: 180px;
    background:
      radial-gradient(
        circle at 16% 20%,
        color-mix(in srgb, var(--gallery-accent) 38%, transparent),
        transparent 28%
      ),
      radial-gradient(
        circle at 82% 26%,
        color-mix(in srgb, var(--gallery-secondary) 34%, transparent),
        transparent 28%
      ),
      linear-gradient(
        145deg,
        color-mix(in srgb, var(--gallery-shadow) 86%, black 14%),
        rgba(10, 13, 17, 0.96)
      );
  }

  .gallery-card__art-grid {
    position: absolute;
    inset: 1rem;
    border-radius: 0.8rem;
    border: 1px solid rgba(255, 255, 255, 0.08);
    background-image:
      linear-gradient(rgba(255, 255, 255, 0.05) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.05) 1px, transparent 1px);
    background-size: 18px 18px;
  }

  .gallery-card__art-mark {
    position: absolute;
    left: 1rem;
    top: 1rem;
    display: inline-flex;
    min-height: 2rem;
    align-items: center;
    padding: 0 0.8rem;
    border-radius: 999px;
    border: 1px solid rgba(255, 255, 255, 0.12);
    background: rgba(10, 13, 17, 0.42);
    color: rgba(255, 255, 255, 0.72);
    font-family: var(--font-mono);
    font-size: 0.62rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }

  .gallery-card__body {
    display: grid;
    gap: 1rem;
    padding: 1rem;
  }

  .gallery-card__copy {
    display: grid;
    gap: 0.45rem;
  }

  .gallery-card__metrics {
    display: grid;
    gap: 0.75rem;
  }

  .gallery-card__metric {
    padding-top: 0.75rem;
    border-top: 1px solid
      color-mix(in srgb, var(--color-border) 62%, transparent);
  }

  .gallery-card__metric strong {
    display: block;
    margin-top: 0.35rem;
    color: var(--color-text-primary);
    font-size: 0.95rem;
    font-weight: 600;
  }

  .gallery-card__notes {
    padding-top: 0.85rem;
    border-top: 1px solid
      color-mix(in srgb, var(--color-border) 62%, transparent);
  }

  .gallery-card__notes summary {
    cursor: pointer;
    list-style: none;
    color: var(--color-text-primary);
    font-family: var(--font-mono);
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  .gallery-card__notes summary::-webkit-details-marker {
    display: none;
  }

  .gallery-card__footer {
    display: grid;
    gap: 0.8rem;
    padding-top: 0.85rem;
    border-top: 1px solid
      color-mix(in srgb, var(--color-border) 62%, transparent);
  }

  .gallery-doctrine-grid {
    gap: 2.5rem;
  }

  .gallery-doctrine-list,
  .gallery-route-list {
    display: grid;
    gap: 1rem;
    margin-top: 1rem;
  }

  .gallery-doctrine-item {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 0.9rem;
    padding: 1rem 0;
    border-bottom: 1px solid
      color-mix(in srgb, var(--color-border) 64%, transparent);
  }

  .gallery-doctrine-item span {
    color: color-mix(in srgb, var(--color-text-primary) 36%, transparent);
    font-family: var(--font-mono);
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.18em;
  }

  .gallery-route-card {
    display: grid;
    gap: 0.45rem;
    padding: 1rem 0;
    border-bottom: 1px solid
      color-mix(in srgb, var(--color-border) 64%, transparent);
    transition:
      border-color 0.2s ease,
      transform 0.2s ease;
  }

  @media (min-width: 960px) {
    .gallery-page-hero__shell,
    .gallery-doctrine-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      align-items: start;
    }

    .gallery-page-signal-list {
      grid-column: 1 / -1;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 1.25rem;
    }

    .gallery-page-signal {
      padding-right: 1rem;
    }

    .gallery-state-bar {
      grid-template-columns: minmax(0, 1fr) auto;
      align-items: end;
    }

    .gallery-grid.is-poster {
      grid-template-columns: repeat(12, minmax(0, 1fr));
    }

    .gallery-grid.is-salon {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .gallery-grid.is-index {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .gallery-card--feature,
    .gallery-card--wide {
      grid-column: span 7;
    }

    .gallery-card--standard,
    .gallery-card--tall {
      grid-column: span 5;
    }
  }

  @media (min-width: 1100px) {
    .gallery-grid.is-poster .gallery-card {
      grid-template-columns: minmax(240px, 0.9fr) minmax(0, 1.1fr);
    }
  }

  @media (max-width: 767px) {
    .gallery-page-hero {
      padding-top: calc(5.8rem + env(safe-area-inset-top));
    }

    .gallery-page-title {
      max-width: 100%;
      font-size: clamp(2.5rem, 12vw, 4.4rem);
    }

    .gallery-page-actions {
      flex-direction: column;
    }
  }
</style>
