---
import MarketingLayout from '../layouts/MarketingLayout.astro';
import ModernCard from '../components/ui/ModernCard.astro';
import ModernButton from '../components/ui/ModernButton.astro';
import { withBasePath } from '../utils/helpers';
import { CONTACT_EMAIL } from '../consts';

const portfolioStats = [
  {
    label: 'Artifact Type',
    value: 'Interactive Commerce Concept',
    note: 'Editorial storefront with cinematic pacing',
  },
  {
    label: 'Build Tempo',
    value: '4 Rapid Sprints',
    note: 'Concept, interface, data model, testing loops',
  },
  {
    label: 'Core Stack',
    value: 'Astro + TS + Islands',
    note: 'Static-first shell with selective interactivity',
  },
  {
    label: 'Current State',
    value: 'Retired From Production',
    note: 'Preserved as a reference piece after direction changed',
  },
];

const projectGoals = [
  {
    title: 'Prototype a story-first storefront',
    description:
      'Blend editorial pacing with practical buying flows so discovery and conversion feel like one continuous scene.',
  },
  {
    title: 'Pressure-test reusable design primitives',
    description:
      'Push cards, navigation shells, and content layouts beyond default templates to prove they could carry future experiments.',
  },
  {
    title: 'Keep speed as a creative constraint',
    description:
      'Deliver visual richness without abandoning Astro’s static strengths, keeping interactions sharp and pages lightweight.',
  },
];

const implementationPhases = [
  {
    phase: 'Act I',
    title: 'Narrative architecture',
    detail:
      'Mapped the journey from landing to checkout like a storyboard, defining rhythm, hierarchy, and trust cues first.',
  },
  {
    phase: 'Act II',
    title: 'Systemized buildout',
    detail:
      'Built reusable primitives and assembled route-level scenes for hero, listing, PDP, cart, and checkout touchpoints.',
  },
  {
    phase: 'Act III',
    title: 'Interaction polish',
    detail:
      'Refined loading, error, and keyboard states until the experience felt deliberate across desktop and mobile breakpoints.',
  },
  {
    phase: 'Act IV',
    title: 'Retirement with intent',
    detail:
      'Removed the demo from live navigation and archived it cleanly so the learnings stayed usable for future creative builds.',
  },
];

const outcomes = [
  'Reusable UX patterns for discovery, filtering, and intent-driven cart progression.',
  'A stronger playbook for cinematic visual treatment within static-first constraints.',
  'A cleaner retirement workflow: archive, redirect, preserve, and keep moving.',
];

const contactHref = `mailto:${encodeURIComponent(
  CONTACT_EMAIL
)}?subject=${encodeURIComponent('Portfolio inquiry')}`;
---

<MarketingLayout
  title="Portfolio | Olive Global Systems"
  description="Portfolio spotlight: the retired Shop Demo as a creative systems case study, from concept to controlled retirement."
>
  <section class="relative overflow-hidden bg-zinc-950" data-reveal>
    <div
      class="absolute inset-0 -z-10 bg-[radial-gradient(circle_at_top,_rgba(0,102,255,0.16),_transparent_55%)]"
    >
    </div>
    <div class="mx-auto max-w-[1220px] px-6 pb-16 pt-28">
      <div class="max-w-4xl space-y-6">
        <p
          class="text-xs font-semibold uppercase tracking-[0.32em] text-zinc-400"
        >
          Portfolio
        </p>
        <h1 class="text-4xl font-bold text-white sm:text-5xl">
          Shop Demo: a concept piece,
          <span class="text-accent-300">
            now archived and repurposed as portfolio evidence.</span
          >
        </h1>
        <p class="text-base text-zinc-300">
          This wasn’t a throwaway mockup. It was a full creative systems drill
          that explored commerce UX, narrative pacing, and reusable component
          architecture. The route is gone from production, but the craft and
          patterns continue to influence the site.
        </p>
        <div class="flex flex-col gap-3 sm:flex-row">
          <ModernButton
            href={withBasePath('services/')}
            variant="secondary"
            className="min-h-[48px]"
          >
            Explore services
          </ModernButton>
          <ModernButton
            href={contactHref}
            variant="primary"
            className="min-h-[48px]"
          >
            Discuss a similar build
          </ModernButton>
        </div>
      </div>
    </div>
  </section>

  <section class="border-y border-white/5 bg-zinc-900/30" data-reveal>
    <div
      class="mx-auto grid max-w-[1220px] gap-4 px-6 py-8 sm:grid-cols-2 lg:grid-cols-4"
    >
      {
        portfolioStats.map(stat => (
          <ModernCard
            variant="minimal"
            className="border-white/10 bg-zinc-950/60 p-5"
          >
            <p class="text-xs uppercase tracking-[0.22em] text-zinc-500">
              {stat.label}
            </p>
            <p class="mt-2 text-2xl font-semibold text-white">{stat.value}</p>
            <p class="mt-1 text-xs text-zinc-400">{stat.note}</p>
          </ModernCard>
        ))
      }
    </div>
  </section>

  <section class="bg-zinc-950" data-reveal>
    <div class="mx-auto max-w-[1220px] px-6 py-20">
      <div class="max-w-3xl space-y-4">
        <h2 class="text-3xl font-semibold text-white">Project goals</h2>
        <p class="text-sm text-zinc-300">
          The goal was to push taste, pacing, and interaction quality without
          sacrificing maintainability.
        </p>
      </div>

      <div class="mt-10 grid gap-5 md:grid-cols-3">
        {
          projectGoals.map(goal => (
            <ModernCard
              variant="minimal"
              className="p-6 bg-zinc-950/60 border-white/10"
            >
              <h3 class="text-lg font-semibold text-white">{goal.title}</h3>
              <p class="mt-3 text-sm leading-relaxed text-zinc-300">
                {goal.description}
              </p>
            </ModernCard>
          ))
        }
      </div>
    </div>
  </section>

  <section class="border-y border-white/5 bg-zinc-900/30" data-reveal>
    <div class="mx-auto max-w-[1220px] px-6 py-20">
      <div class="max-w-3xl space-y-4">
        <h2 class="text-3xl font-semibold text-white">Creative timeline</h2>
        <p class="text-sm text-zinc-300">
          Four acts from concept to delivery to sunset, each with clear intent.
        </p>
      </div>

      <div class="mt-10 grid gap-4">
        {
          implementationPhases.map(item => (
            <ModernCard
              variant="minimal"
              className="p-6 bg-zinc-950/60 border-white/10"
            >
              <div class="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between">
                <div>
                  <p class="text-xs font-semibold uppercase tracking-[0.3em] text-zinc-400">
                    {item.phase}
                  </p>
                  <p class="mt-2 text-base font-semibold text-white">
                    {item.title}
                  </p>
                  <p class="mt-2 text-sm text-zinc-300">{item.detail}</p>
                </div>
              </div>
            </ModernCard>
          ))
        }
      </div>
    </div>
  </section>

  <section class="bg-zinc-950" data-reveal>
    <div class="mx-auto max-w-[1220px] px-6 py-20">
      <div class="rounded-2xl border border-white/10 bg-white/5 p-8">
        <div class="grid gap-8 lg:grid-cols-[1.2fr_1fr] lg:items-center">
          <div>
            <h2 class="text-2xl font-semibold text-white">
              What survived the sunset
            </h2>
            <p class="mt-3 text-sm text-zinc-300">
              The route is retired, but the project left behind stronger UI
              primitives, better interaction standards, and cleaner delivery
              mechanics.
            </p>
            <ul class="mt-4 space-y-2 text-sm text-zinc-200">
              {
                outcomes.map(outcome => (
                  <li class="flex items-start gap-2">
                    <span class="mt-2 h-1.5 w-1.5 flex-shrink-0 bg-accent-500" />
                    <span>{outcome}</span>
                  </li>
                ))
              }
            </ul>
          </div>
          <div class="flex flex-col gap-3 sm:flex-row lg:flex-col">
            <ModernButton
              href={withBasePath('contact-hq/')}
              variant="primary"
              className="min-h-[48px]"
            >
              Start project intake
            </ModernButton>
            <ModernButton
              href={contactHref}
              variant="secondary"
              className="min-h-[48px]"
            >
              Email portfolio questions
            </ModernButton>
          </div>
        </div>
      </div>
    </div>
  </section>
</MarketingLayout>
