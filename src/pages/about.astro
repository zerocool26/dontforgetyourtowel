---
import MarketingLayout from '../layouts/MarketingLayout.astro';
import ModernCard from '../components/ui/ModernCard.astro';
import ModernButton from '../components/ui/ModernButton.astro';
import { withBasePath } from '../utils/helpers';
import { CONTACT_EMAIL } from '../consts';

const portfolioStats = [
  {
    label: 'Project Type',
    value: 'Commerce Demo',
    note: 'High-fidelity storefront prototype',
  },
  {
    label: 'Build Window',
    value: '4 Sprints',
    note: 'Design, UX, data modeling, QA',
  },
  {
    label: 'Primary Stack',
    value: 'Astro + TS',
    note: 'Static-first architecture with reusable components',
  },
  {
    label: 'Status',
    value: 'Retired',
    note: 'Archived as a portfolio artifact after scope shift',
  },
];

const projectGoals = [
  {
    title: 'Prove conversion-focused UX',
    description:
      'Validate category browsing, product discovery, and checkout-oriented CTA patterns inside a realistic demo journey.',
  },
  {
    title: 'Stress-test component system',
    description:
      'Exercise reusable cards, navigation, and content layouts that could be repurposed across future product pages.',
  },
  {
    title: 'Demonstrate static performance discipline',
    description:
      'Ship an experience with strong perceived speed while maintaining maintainable Astro-first implementation patterns.',
  },
];

const implementationPhases = [
  {
    phase: 'Phase 01',
    title: 'Experience blueprint',
    detail:
      'Defined catalog flow, product detail patterns, and trust messaging with a mobile-first navigation plan.',
  },
  {
    phase: 'Phase 02',
    title: 'Frontend implementation',
    detail:
      'Built reusable UI primitives and route-level composition for hero, listing, PDP, cart, and checkout touchpoints.',
  },
  {
    phase: 'Phase 03',
    title: 'Interaction hardening',
    detail:
      'Refined empty/loading/error states, keyboard behavior, and responsive edge cases across key storefront interactions.',
  },
  {
    phase: 'Phase 04',
    title: 'Sunset and archive',
    detail:
      'Retired the demo from production navigation and preserved the implementation as a reference for future portfolio reviews.',
  },
];

const outcomes = [
  'Documented reusable UX patterns for search, filtering, and cart progression.',
  'Captured static-delivery learnings to inform future microsite and demo builds.',
  'Established a cleaner decommissioning workflow with backup and redirect strategy.',
];

const contactHref = `mailto:${encodeURIComponent(
  CONTACT_EMAIL
)}?subject=${encodeURIComponent('Portfolio inquiry')}`;
---

<MarketingLayout
  title="Portfolio | Olive Global Systems"
  description="Portfolio spotlight: the retired Shop Demo and the implementation patterns it contributed to our current platform."
>
  <section class="relative overflow-hidden bg-zinc-950" data-reveal>
    <div
      class="absolute inset-0 -z-10 bg-[radial-gradient(circle_at_top,_rgba(0,102,255,0.16),_transparent_55%)]"
    >
    </div>
    <div class="mx-auto max-w-[1220px] px-6 pb-16 pt-28">
      <div class="max-w-4xl space-y-6">
        <p
          class="text-xs font-semibold uppercase tracking-[0.32em] text-zinc-400"
        >
          Portfolio
        </p>
        <h1 class="text-4xl font-bold text-white sm:text-5xl">
          Shop Demo Retrospective.
          <span class="text-accent-300">
            A production-style prototype that now lives as an artifact.</span
          >
        </h1>
        <p class="text-base text-zinc-300">
          The former Shop Demo route has been retired from the live site, but
          the work remains useful as a portfolio case study. This page captures
          what was built, why it mattered, and which patterns continue to shape
          the current product experience.
        </p>
        <div class="flex flex-col gap-3 sm:flex-row">
          <ModernButton
            href={withBasePath('services/')}
            variant="secondary"
            className="min-h-[48px]"
          >
            Explore services
          </ModernButton>
          <ModernButton
            href={contactHref}
            variant="primary"
            className="min-h-[48px]"
          >
            Discuss a similar build
          </ModernButton>
        </div>
      </div>
    </div>
  </section>

  <section class="border-y border-white/5 bg-zinc-900/30" data-reveal>
    <div
      class="mx-auto grid max-w-[1220px] gap-4 px-6 py-8 sm:grid-cols-2 lg:grid-cols-4"
    >
      {
        portfolioStats.map(stat => (
          <ModernCard
            variant="minimal"
            className="border-white/10 bg-zinc-950/60 p-5"
          >
            <p class="text-xs uppercase tracking-[0.22em] text-zinc-500">
              {stat.label}
            </p>
            <p class="mt-2 text-2xl font-semibold text-white">{stat.value}</p>
            <p class="mt-1 text-xs text-zinc-400">{stat.note}</p>
          </ModernCard>
        ))
      }
    </div>
  </section>

  <section class="bg-zinc-950" data-reveal>
    <div class="mx-auto max-w-[1220px] px-6 py-20">
      <div class="max-w-3xl space-y-4">
        <h2 class="text-3xl font-semibold text-white">Project goals</h2>
        <p class="text-sm text-zinc-300">
          The demo was intentionally scoped as a high-signal implementation
          exercise, not a long-term product destination.
        </p>
      </div>

      <div class="mt-10 grid gap-5 md:grid-cols-3">
        {
          projectGoals.map(goal => (
            <ModernCard
              variant="minimal"
              className="p-6 bg-zinc-950/60 border-white/10"
            >
              <h3 class="text-lg font-semibold text-white">{goal.title}</h3>
              <p class="mt-3 text-sm leading-relaxed text-zinc-300">
                {goal.description}
              </p>
            </ModernCard>
          ))
        }
      </div>
    </div>
  </section>

  <section class="border-y border-white/5 bg-zinc-900/30" data-reveal>
    <div class="mx-auto max-w-[1220px] px-6 py-20">
      <div class="max-w-3xl space-y-4">
        <h2 class="text-3xl font-semibold text-white">
          Implementation timeline
        </h2>
        <p class="text-sm text-zinc-300">
          A concise delivery sequence that moved from concept to finished demo
          and then into controlled retirement.
        </p>
      </div>

      <div class="mt-10 grid gap-4">
        {
          implementationPhases.map(item => (
            <ModernCard
              variant="minimal"
              className="p-6 bg-zinc-950/60 border-white/10"
            >
              <div class="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between">
                <div>
                  <p class="text-xs font-semibold uppercase tracking-[0.3em] text-zinc-400">
                    {item.phase}
                  </p>
                  <p class="mt-2 text-base font-semibold text-white">
                    {item.title}
                  </p>
                  <p class="mt-2 text-sm text-zinc-300">{item.detail}</p>
                </div>
              </div>
            </ModernCard>
          ))
        }
      </div>
    </div>
  </section>

  <section class="bg-zinc-950" data-reveal>
    <div class="mx-auto max-w-[1220px] px-6 py-20">
      <div class="rounded-2xl border border-white/10 bg-white/5 p-8">
        <div class="grid gap-8 lg:grid-cols-[1.2fr_1fr] lg:items-center">
          <div>
            <h2 class="text-2xl font-semibold text-white">
              What carried forward
            </h2>
            <p class="mt-3 text-sm text-zinc-300">
              Even though the storefront route is gone, the project improved our
              component quality, test strategy, and delivery playbooks.
            </p>
            <ul class="mt-4 space-y-2 text-sm text-zinc-200">
              {
                outcomes.map(outcome => (
                  <li class="flex items-start gap-2">
                    <span class="mt-2 h-1.5 w-1.5 flex-shrink-0 bg-accent-500" />
                    <span>{outcome}</span>
                  </li>
                ))
              }
            </ul>
          </div>
          <div class="flex flex-col gap-3 sm:flex-row lg:flex-col">
            <ModernButton
              href={withBasePath('contact-hq/')}
              variant="primary"
              className="min-h-[48px]"
            >
              Start project intake
            </ModernButton>
            <ModernButton
              href={contactHref}
              variant="secondary"
              className="min-h-[48px]"
            >
              Email portfolio questions
            </ModernButton>
          </div>
        </div>
      </div>
    </div>
  </section>
</MarketingLayout>
