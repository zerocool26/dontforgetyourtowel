---
import MarketingLayout from '../layouts/MarketingLayout.astro';
import ModernCard from '../components/ui/ModernCard.astro';
import ModernButton from '../components/ui/ModernButton.astro';
import RevealOnScroll from '../components/ui/RevealOnScroll.astro';
import BackgroundEffects from '../components/ui/BackgroundEffects.astro';
import CSS3DScene from '../components/ui/CSS3DScene.astro';
import Advanced3DShowcase from '../components/ui/Advanced3DShowcase.astro';
import CubicMatrix from '../components/ui/CubicMatrix.astro';
import HolographicCards from '../components/ui/HolographicCards.astro';
import DNAHelix from '../components/ui/DNAHelix.astro';
import InfinityTunnel from '../components/ui/InfinityTunnel.astro';
import FloatingIslands from '../components/ui/FloatingIslands.astro';
import ParticleVortex from '../components/ui/ParticleVortex.astro';
import LayeredCards from '../components/ui/LayeredCards.astro';
import ExplodingGrid from '../components/ui/ExplodingGrid.astro';
import PrismaticSphere from '../components/ui/PrismaticSphere.astro';
import MatrixRain from '../components/ui/MatrixRain.astro';
import { withBasePath } from '../utils/helpers';
import LegacyRedirect from '../components/LegacyRedirect.astro';

const title = 'Visual Showcase - Interactive 2D & 3D Demonstrations';
const description =
  'Experience cutting-edge animations, 3D graphics, and modern web design in action';
const navigationLinks = [
  { href: '#advanced-3d', label: 'Advanced 3D' },
  { href: '#cubic-matrix', label: 'Cubic Matrix' },
  { href: '#holographic', label: 'Holographic' },
  { href: '#dna-helix', label: 'DNA Helix' },
  { href: '#infinity-tunnel', label: 'Infinity Tunnel' },
  { href: '#floating-islands', label: 'Floating Islands' },
  { href: '#particle-vortex', label: 'Particle Vortex' },
  { href: '#layered-cards', label: 'Layered Cards' },
  { href: '#exploding-grid', label: 'Exploding Grid' },
  { href: '#prismatic-sphere', label: 'Prismatic Sphere' },
  { href: '#matrix-rain', label: 'Matrix Rain' },
];
const heroStats = [
  {
    label: 'Live canvases',
    value: '12',
    detail: 'GPU-accelerated, CSS-only modules',
  },
  {
    label: 'Latency added',
    value: '0kb JS',
    detail: 'Hydration-free motion layers',
  },
  {
    label: 'Use cases',
    value: '6 playbooks',
    detail: 'Ops, GTM, and research ready',
  },
];
const functionalityIdeas = [
  {
    icon: '🧭',
    title: 'Executive launch board',
    summary:
      'Layer CSS3D scenes behind go-to-market status cards so executives can grasp trajectory and risk in one glance.',
    detail:
      'Blend gradient lighting with KPIs for a calm but cinematic control surface.',
    badge: 'Leadership Ready',
  },
  {
    icon: '⚙️',
    title: 'Incident runway visualizer',
    summary:
      'Replay remediation timelines with Advanced3DShowcase to illustrate detection, mitigation, and recovery states.',
    detail: 'Color-code cube faces so SRE and security teams align instantly.',
    badge: 'SRE + Security',
  },
  {
    icon: '📐',
    title: 'Product story deck',
    summary:
      'Combine LayeredCards and HolographicCards to create interactive slides directly inside static launch docs.',
    detail:
      'Keep typography synced with MarketingLayout tokens for continuity.',
    badge: 'GTM Enablement',
  },
  {
    icon: '🧪',
    title: 'Research lab interactions',
    summary:
      'FloatingIslands and ParticleVortex illustrate architecture experiments without pulling in WebGL.',
    detail: 'Pure CSS keeps prototypes deployable on GitHub Pages.',
    badge: 'No WebGL',
  },
];
const integrationCombos = [
  {
    title: 'Ops dashboards + Spatial overlays',
    summary:
      'Embed InfinityTunnel near /dashboard-v2 metrics so throughput changes are obvious even before trendlines render.',
    tags: ['Dashboards', 'Status'],
    impact: 'Highlights macro-state shifts without extra bundles.',
    link: {
      label: 'View dashboard blueprint',
      href: withBasePath('dashboard-v2'),
    },
  },
  {
    title: 'Client portal storyboards',
    summary:
      'Pair LayeredCards with case studies on /services to keep customer updates tactile and premium.',
    tags: ['Client Portal', 'Story'],
    impact: 'Feels bespoke while staying static-export friendly.',
    link: {
      label: 'Open services narrative',
      href: withBasePath('services'),
    },
  },
  {
    title: 'Contact flows with motion cues',
    summary:
      'Use ParticleVortex cues and ModernButton neon states inside /contact to direct attention during conversions.',
    tags: ['Marketing', 'Acquisition'],
    impact: 'Directional motion nudges action without overwhelming the form.',
    link: {
      label: 'Visit contact orchestration',
      href: withBasePath('contact'),
    },
  },
  {
    title: 'Research releases with ambience',
    summary:
      'Frame monthly blog drops with MatrixRain or PrismaticSphere to keep analysis pieces on-brand.',
    tags: ['Research', 'Content'],
    impact: 'Maintains analytical tone while adding intrigue.',
    link: {
      label: 'Browse the blog index',
      href: withBasePath('blog'),
    },
  },
];
const pipelineTracks = [
  {
    title: 'Spatial narratives',
    subtitle: 'Hero > Story > CTA',
    description:
      'Blend CSS3D scenes with MarketingLayout sections to hand off living narratives without touching WebGL.',
    link: {
      label: 'Map to /about',
      href: withBasePath('about'),
    },
    chip: 'Story',
  },
  {
    title: 'Incident playback',
    subtitle: 'Deck > Timeline > Dashboard',
    description:
      'Chain Advanced3DShowcase, LayeredCards, and dashboard KPIs to replay mitigation steps in one frame.',
    link: {
      label: 'Open /dashboard',
      href: withBasePath('dashboard'),
    },
    chip: 'Reliability',
  },
  {
    title: 'Client immersion',
    subtitle: 'Portal > Case studies',
    description:
      'Use HolographicCards with testimonial callouts so every client update feels tactile yet static-hosted.',
    link: {
      label: 'Preview /services',
      href: withBasePath('services'),
    },
    chip: 'GTM',
  },
  {
    title: 'Research labs',
    subtitle: 'Blog > Experiments',
    description:
      'MatrixRain + ParticleVortex keep deep-dive posts cinematic while staying Markdown-first.',
    link: {
      label: 'View /blog',
      href: withBasePath('blog'),
    },
    chip: 'Analysis',
  },
];
const immersionTimeline = [
  {
    id: '00',
    title: 'Signal ingestion',
    description:
      'Collect KPI snapshots, transcripts, and mood boards inside Markdown. The showcase reads those inputs statically.',
    state: 'Now',
  },
  {
    id: '01',
    title: 'Spatial scoring',
    description:
      'CubicMatrix + DNAHelix rank each brief for dimensional storytelling, lighting, and texture needs.',
    state: 'Processing',
  },
  {
    id: '02',
    title: 'Immersion rehearsal',
    description:
      'FloatingIslands, InfinityTunnel, and LayeredCards render variants without hydration, ready for stakeholder review.',
    state: 'In review',
  },
  {
    id: '03',
    title: 'Launch + telemetry',
    description:
      'Advanced3DShowcase publishes, WebVitals tracks, and the same canvases travel to /demo and /services routes.',
    state: 'Ship',
  },
];
const materialSwatches = [
  {
    name: 'Iridescent mesh',
    tones: ['#d946ef', '#8b5cf6', '#38bdf8'],
    usage: 'Hero gradients & holographic cards',
    texture: 'mesh',
  },
  {
    name: 'Signal noir',
    tones: ['#09090b', '#18181b', '#3f3f46'],
    usage: 'Backgrounds & typography contrast',
    texture: 'grain',
  },
  {
    name: 'Lumen glass',
    tones: ['#f8fafc', '#d1d5db', '#a5b4fc'],
    usage: 'Glassmorphism cards & inputs',
    texture: 'glass',
  },
  {
    name: 'Vortex pulse',
    tones: ['#f97316', '#fb7185', '#fde047'],
    usage: 'Accent trails & loading states',
    texture: 'pulse',
  },
];
---

<LegacyRedirect
  destination="/"
  reason="This page was part of the original starter template and has been removed."
/>

<!--
<MarketingLayout
  title={title}
  description={description}
  bodyClass="bg-neutral-950 text-white"
  mainClass="relative overflow-hidden space-y-32 pb-36"
>
  <div
    aria-hidden="true"
    class="pointer-events-none absolute inset-0 opacity-70"
  >
    <BackgroundEffects variant="mesh" intensity="subtle" animated blur="xl" />
  </div>

  <section class="relative pt-24">
    <div class="relative mx-auto max-w-[1220px] px-6">
      <div
        class="grid gap-12 lg:grid-cols-[minmax(0,1.05fr)_minmax(0,0.95fr)] items-center"
      >
        <div>
          <p
            class="text-xs font-semibold uppercase tracking-[0.32em] text-slate-400"
          >
            Visual systems lab
          </p>
          <h1 class="text-4xl sm:text-6xl font-semibold text-white mt-6">
            Visual Showcase
          </h1>
          <p class="mt-6 text-lg text-slate-300 max-w-2xl">
            A modern, operations-ready gallery of CSS-only canvases, animation
            systems, and tactile UI patterns. Each module ships without
            hydration yet feels like a custom build for leadership decks, GTM
            motion, and analyst briefings.
          </p>
          <div class="mt-8 flex flex-wrap gap-4">
            <ModernButton variant="neon" size="xl" shadow="xl">
              Start exploring
            </ModernButton>
            <a
              href={withBasePath('ultimate-3d-gallery')}
              class="inline-flex items-center gap-2 rounded-full border border-white/15 px-7 py-3 text-sm font-semibold text-white transition hover:border-purple-400/70 hover:text-purple-100"
            >
              🚀 Ultimate gallery
            </a>
          </div>
          <dl class="mt-10 grid gap-4 sm:grid-cols-3">
            {
              heroStats.map(stat => (
                <ModernCard
                  variant="glass"
                  className="bg-white/[0.03] p-5 border-white/10"
                >
                  <dt class="text-[0.65rem] uppercase tracking-[0.32em] text-slate-400">
                    {stat.label}
                  </dt>
                  <dd class="mt-2 text-2xl font-semibold text-white">
                    {stat.value}
                  </dd>
                  <p class="mt-1 text-sm text-slate-400">{stat.detail}</p>
                </ModernCard>
              ))
            }
          </dl>
        </div>
        <div class="relative">
          <div
            class="absolute -inset-6 rounded-[36px] bg-gradient-to-r from-purple-600/30 via-pink-500/20 to-blue-500/30 blur-3xl"
            aria-hidden="true"
          >
          </div>
          <div
            class="relative rounded-[32px] border border-white/10 bg-black/40 p-4 backdrop-blur"
          >
            <CSS3DScene />
          </div>
        </div>
      </div>

      <div class="mt-16 rounded-2xl border border-white/10 bg-white/[0.02] p-6">
        <p
          class="text-center text-[0.65rem] uppercase tracking-[0.32em] text-slate-500"
        >
          Jump to section
        </p>
        <div class="mt-4 flex flex-wrap justify-center gap-3 text-sm">
          {
            navigationLinks.map(link => (
              <a
                href={link.href}
                class="rounded-full border border-white/10 px-4 py-2 text-slate-200 transition hover:border-purple-400/70 hover:text-white"
              >
                {link.label}
              </a>
            ))
          }
        </div>
      </div>
    </div>
  </section>

  <section class="relative">
    <div
      class="absolute inset-0 -z-10 bg-[radial-gradient(circle_at_top,_rgba(99,102,241,0.15),_transparent_60%)]"
      aria-hidden="true"
    >
    </div>
    <div
      class="relative mx-auto max-w-[1220px] px-6 lg:grid lg:grid-cols-[minmax(0,0.85fr)_minmax(0,1.15fr)] lg:items-start lg:gap-12"
    >
      <div class="space-y-6">
        <p
          class="text-xs font-semibold uppercase tracking-[0.32em] text-slate-400"
        >
          Functionality ideas
        </p>
        <h2 class="text-3xl sm:text-4xl font-semibold text-white">
          Drop these canvases into the same executive narratives as the rest of
          the site
        </h2>
        <p class="text-base text-slate-300">
          Every effect stays compliant with the design system already used on
          the marketing, dashboard, and operations pages. Mix and match without
          retooling typography, spacing, or hosting infrastructure.
        </p>
      </div>
      <div class="mt-12 grid gap-6 md:grid-cols-2 lg:mt-0">
        {
          functionalityIdeas.map(idea => (
            <ModernCard
              variant="glass"
              className="space-y-3 bg-white/[0.03] p-6 border-white/10"
            >
              <div class="flex items-center justify-between gap-3">
                <span class="text-2xl" aria-hidden="true">
                  {idea.icon}
                </span>
                <span class="text-[0.58rem] uppercase tracking-[0.32em] text-slate-400">
                  {idea.badge}
                </span>
              </div>
              <h3 class="text-xl font-semibold text-white">{idea.title}</h3>
              <p class="text-sm text-slate-300">{idea.summary}</p>
              <p class="text-xs text-slate-500">{idea.detail}</p>
            </ModernCard>
          ))
        }
      </div>
    </div>
  </section>

  <section
    class="relative border-y border-white/5 bg-white/[0.02] py-20 backdrop-blur"
  >
    <div class="mx-auto max-w-[1220px] px-6 space-y-10">
      <div class="max-w-3xl space-y-4">
        <p
          class="text-xs font-semibold uppercase tracking-[0.32em] text-slate-400"
        >
          Pairings & integrations
        </p>
        <h2 class="text-3xl font-semibold text-white">
          Plug the visual showcase into other pages without rethinking
          storytelling
        </h2>
        <p class="text-base text-slate-300">
          Each combo mirrors a live route elsewhere on the site so product,
          marketing, and engineering teams can reuse the same stack.
        </p>
      </div>
      <div class="grid gap-6 md:grid-cols-2">
        {
          integrationCombos.map(combo => (
            <ModernCard
              variant="glass"
              className="bg-black/30 p-6 shadow-[0_15px_45px_rgba(0,0,0,0.35)] border-white/10"
            >
              <div class="mb-4 flex flex-wrap gap-2">
                {combo.tags.map(tag => (
                  <span class="rounded-full border border-white/10 px-3 py-1 text-[0.65rem] uppercase tracking-[0.28em] text-slate-300">
                    {tag}
                  </span>
                ))}
              </div>
              <h3 class="text-2xl font-semibold text-white">{combo.title}</h3>
              <p class="mt-3 text-sm text-slate-300">{combo.summary}</p>
              <div class="mt-6 flex flex-wrap items-center justify-between gap-4">
                <p class="text-xs text-slate-500">{combo.impact}</p>
                <a
                  href={combo.link.href}
                  class="inline-flex items-center gap-2 text-sm font-semibold text-purple-300 transition hover:text-white"
                >
                  {combo.link.label}
                  <span aria-hidden="true">↗</span>
                </a>
              </div>
            </ModernCard>
          ))
        }
      </div>
    </div>
  </section>

  <section class="relative py-24">
    <div class="mx-auto max-w-[1220px] px-6 space-y-12">
      <div
        class="grid gap-10 lg:grid-cols-[minmax(0,0.55fr)_minmax(0,1.45fr)] lg:items-start"
      >
        <div class="space-y-4">
          <p
            class="text-xs font-semibold uppercase tracking-[0.32em] text-slate-400"
          >
            Studio pipelines
          </p>
          <h2 class="text-3xl font-semibold text-white">
            Move from kickoff to cinematic delivery with reusable tracks
          </h2>
          <p class="text-base text-slate-300">
            Each pipeline maps to a live page so ops, GTM, and research teams
            can preview the full journey before the first deck export.
          </p>
          <div
            class="flex flex-wrap gap-3 text-[0.65rem] uppercase tracking-[0.32em] text-slate-400"
          >
            <span class="rounded-full border border-white/10 px-4 py-1"
              >Storytelling</span
            >
            <span class="rounded-full border border-white/10 px-4 py-1"
              >Ops orchestration</span
            >
            <span class="rounded-full border border-white/10 px-4 py-1"
              >Launch readiness</span
            >
          </div>
        </div>
        <div class="grid gap-6 lg:grid-cols-2">
          {
            pipelineTracks.map(track => (
              <ModernCard
                variant="glass"
                className="group relative overflow-hidden rounded-3xl bg-white/[0.03] p-6 border-white/10"
              >
                <div
                  class="absolute inset-px rounded-[26px] bg-gradient-to-br from-white/5 via-transparent to-transparent opacity-0 transition group-hover:opacity-100"
                  aria-hidden="true"
                />
                <div class="relative space-y-4">
                  <div class="flex items-center justify-between text-xs uppercase tracking-[0.32em] text-slate-400">
                    <span>{track.subtitle}</span>
                    <span class="rounded-full border border-white/10 px-3 py-1 text-[0.58rem] tracking-[0.32em]">
                      {track.chip}
                    </span>
                  </div>
                  <h3 class="text-2xl font-semibold text-white">
                    {track.title}
                  </h3>
                  <p class="text-sm text-slate-300">{track.description}</p>
                  <a
                    href={track.link.href}
                    class="inline-flex items-center gap-2 text-sm font-semibold text-purple-300 transition group-hover:text-white"
                  >
                    {track.link.label}
                    <span aria-hidden="true">↗</span>
                  </a>
                </div>
              </ModernCard>
            ))
          }
        </div>
      </div>
    </div>
  </section>

  <section class="relative border-y border-white/5 bg-neutral-950/60 py-24">
    <div
      class="absolute inset-0 bg-[radial-gradient(circle_farther_at_top,_rgba(14,116,144,0.18),_transparent_70%)]"
      aria-hidden="true"
    >
    </div>
    <div class="relative mx-auto max-w-[1220px] px-6">
      <div
        class="grid gap-12 lg:grid-cols-[minmax(0,0.9fr)_minmax(0,1.1fr)] lg:items-center"
      >
        <div class="space-y-4">
          <p
            class="text-xs font-semibold uppercase tracking-[0.32em] text-slate-400"
          >
            Immersion timeline
          </p>
          <h2 class="text-3xl font-semibold text-white">
            From first signal to launch telemetry
          </h2>
          <p class="text-base text-slate-300">
            The same CSS canvases progress through ingestion, rehearsal, and
            launch so every stakeholder shares a single storyboard.
          </p>
        </div>
        <div class="space-y-4">
          {
            immersionTimeline.map((phase, idx) => (
              <ModernCard
                variant="glass"
                className={`flex flex-col gap-3 rounded-3xl border-white/10 ${idx % 2 ? 'bg-white/[0.04]' : 'bg-white/[0.08]'} p-5 lg:flex-row lg:items-center`}
              >
                <div class="flex items-center gap-4">
                  <span class="text-3xl font-semibold text-white/80">
                    {phase.id}
                  </span>
                  <div class="h-10 w-px bg-white/10" aria-hidden="true" />
                </div>
                <div class="flex-1 space-y-1">
                  <p class="text-sm uppercase tracking-[0.32em] text-slate-400">
                    {phase.state}
                  </p>
                  <h3 class="text-xl font-semibold text-white">
                    {phase.title}
                  </h3>
                  <p class="text-sm text-slate-300">{phase.description}</p>
                </div>
              </ModernCard>
            ))
          }
        </div>
      </div>
    </div>
  </section>

  <section class="relative py-24">
    <div
      class="absolute inset-0 bg-[linear-gradient(120deg,rgba(168,85,247,0.15),rgba(56,189,248,0.12),transparent_70%)]"
      aria-hidden="true"
    >
    </div>
    <div class="relative mx-auto max-w-[1220px] px-6 space-y-10">
      <div class="space-y-4 text-center">
        <p
          class="text-xs font-semibold uppercase tracking-[0.32em] text-slate-400"
        >
          Material swatchboard
        </p>
        <h2 class="text-3xl font-semibold text-white">
          Palettes that keep holographic UI grounded
        </h2>
        <p class="text-base text-slate-300 max-w-3xl mx-auto">
          Mix iridescent gradients, noir foundations, and luminous glass accents
          to match every showcase route without touching theme.ts.
        </p>
      </div>
      <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-4">
        {
          materialSwatches.map(swatch => (
            <ModernCard
              variant="glass"
              className="rounded-[28px] bg-white/[0.04] p-5 border-white/10"
            >
              <div class="mb-4 flex items-center justify-between text-xs uppercase tracking-[0.32em] text-slate-400">
                <span>{swatch.texture}</span>
                <span>{swatch.usage}</span>
              </div>
              <h3 class="text-xl font-semibold text-white">{swatch.name}</h3>
              <div class="mt-4 flex gap-3">
                {swatch.tones.map(tone => (
                  <span
                    class="h-12 w-12 rounded-2xl border border-white/10"
                    style={`background:${tone}`}
                    aria-label={`Color swatch ${tone}`}
                  />
                ))}
              </div>
              <p class="mt-4 text-xs text-slate-400">{swatch.usage}</p>
            </ModernCard>
          ))
        }
      </div>
    </div>
  </section>

  <!-- Advanced 3D Showcase - Maximum Capabilities -->
<section
  id="advanced-3d"
  class="relative bg-gradient-to-b from-neutral-950 via-indigo-950/20 to-neutral-900 py-32"
>
  <div class="absolute inset-0 opacity-20">
    <div
      class="absolute left-1/4 top-0 h-96 w-96 animate-pulse rounded-full bg-purple-500 blur-[128px] filter"
    >
    </div>
    <div
      class="absolute bottom-0 right-1/4 h-96 w-96 animate-pulse rounded-full bg-blue-500 blur-[128px] filter"
      style="animation-delay: 1s"
    >
    </div>
  </div>
  <div class="container relative z-10 mx-auto px-6">
    <RevealOnScroll animation="slide-up">
      <div class="mb-20 text-center">
        <div
          class="mb-6 inline-block rounded-full border border-purple-500/30 bg-purple-500/10 px-6 py-3 backdrop-blur-sm"
        >
          <span
            class="text-sm font-semibold uppercase tracking-wider text-purple-300"
            >Pure CSS • No JavaScript</span
          >
        </div>
        <h2
          class="mb-8 bg-gradient-to-r from-purple-400 via-pink-400 to-blue-400 bg-clip-text text-center text-8xl font-black text-transparent md:text-9xl"
        >
          3D Mastery
        </h2>
        <p
          class="mx-auto max-w-4xl text-center text-2xl leading-relaxed text-neutral-300 md:text-3xl"
        >
          Experience the absolute limits of CSS 3D transforms, perspective
          depth, and GPU-accelerated animations
        </p>
      </div>
    </RevealOnScroll>

    <Advanced3DShowcase />
  </div>
</section>

<!-- Cubic Matrix Section -->
<section
  id="cubic-matrix"
  class="relative bg-gradient-to-b from-neutral-900 to-neutral-950 py-32"
>
  <div
    class="absolute inset-0 bg-[radial-gradient(circle_at_center,rgba(99,102,241,0.1),transparent_70%)]"
  >
  </div>
  <div class="relative z-10">
    <CubicMatrix />
  </div>
</section>

<!-- Holographic Cards Section -->
<section
  id="holographic"
  class="relative bg-gradient-to-b from-neutral-950 via-purple-950/10 to-neutral-900 py-32"
>
  <div class="absolute inset-0">
    <div
      class="absolute left-0 top-1/2 h-[500px] w-[500px] rounded-full bg-purple-600 opacity-20 blur-[150px] filter"
    >
    </div>
    <div
      class="absolute right-0 top-1/2 h-[500px] w-[500px] rounded-full bg-pink-600 opacity-20 blur-[150px] filter"
    >
    </div>
  </div>
  <div class="relative z-10">
    <HolographicCards />
  </div>
</section>

<!-- DNA Helix Section -->
<section
  id="dna-helix"
  class="relative bg-gradient-to-b from-neutral-900 to-black py-32"
>
  <div class="absolute inset-0">
    <div
      class="absolute inset-0 bg-[url('data:image/svg+xml;base64,')] opacity-30"
    >
    </div>
  </div>
  <div class="relative z-10">
    <DNAHelix />
  </div>
</section>

<!-- Infinity Tunnel Section -->
<section
  id="infinity-tunnel"
  class="relative bg-gradient-to-b from-black via-blue-950/10 to-neutral-900 py-32"
>
  <div class="absolute inset-0">
    <div
      class="absolute left-1/2 top-1/2 h-[800px] w-[800px] -translate-x-1/2 -translate-y-1/2 transform animate-pulse rounded-full bg-blue-600 opacity-10 blur-[200px] filter"
    >
    </div>
  </div>
  <div class="relative z-10">
    <InfinityTunnel />
  </div>
</section>

<!-- Floating Islands Section -->
<section
  id="floating-islands"
  class="relative bg-gradient-to-b from-neutral-900 to-neutral-950 py-32"
>
  <div class="absolute inset-0 overflow-hidden">
    <div
      class="absolute left-1/4 top-0 h-[600px] w-[600px] rounded-full bg-cyan-600 opacity-10 blur-[180px] filter"
    >
    </div>
    <div
      class="absolute bottom-0 right-1/4 h-[600px] w-[600px] rounded-full bg-purple-600 opacity-10 blur-[180px] filter"
    >
    </div>
  </div>
  <div class="relative z-10">
    <FloatingIslands />
  </div>
</section>

<!-- Particle Vortex Section -->
<section
  id="particle-vortex"
  class="relative bg-gradient-to-b from-neutral-950 via-purple-950/10 to-black py-32"
>
  <div class="absolute inset-0">
    <div
      class="absolute inset-0 animate-pulse bg-gradient-radial from-purple-900/20 via-transparent to-transparent"
      style="animation-duration: 8s"
    >
    </div>
  </div>
  <div class="relative z-10">
    <ParticleVortex />
  </div>
</section>

<!-- Layered Cards Section -->
<section
  id="layered-cards"
  class="relative bg-gradient-to-b from-black to-neutral-900 py-32"
>
  <div class="absolute inset-0">
    <div
      class="absolute left-0 top-1/3 h-[500px] w-[500px] rounded-full bg-indigo-600 opacity-15 blur-[150px] filter"
    >
    </div>
    <div
      class="absolute bottom-1/3 right-0 h-[500px] w-[500px] rounded-full bg-violet-600 opacity-15 blur-[150px] filter"
    >
    </div>
  </div>
  <div class="relative z-10">
    <LayeredCards />
  </div>
</section>

<!-- Exploding Grid Section -->
<section
  id="exploding-grid"
  class="relative bg-gradient-to-b from-neutral-900 via-pink-950/10 to-neutral-950 py-32"
>
  <div class="absolute inset-0">
    <div
      class="absolute inset-0 bg-[radial-gradient(circle_at_center,rgba(236,72,153,0.15),transparent_70%)]"
    >
    </div>
  </div>
  <div class="relative z-10">
    <ExplodingGrid />
  </div>
</section>

<!-- Prismatic Sphere Section -->
<section
  id="prismatic-sphere"
  class="relative bg-gradient-to-b from-neutral-950 to-black py-32"
>
  <div class="absolute inset-0 overflow-hidden">
    <div
      class="absolute left-1/2 top-1/2 h-[1000px] w-[1000px] -translate-x-1/2 -translate-y-1/2 transform animate-spin rounded-full bg-gradient-conic from-purple-600 via-pink-600 to-purple-600 opacity-20 blur-[250px] filter"
      style="animation-duration: 20s"
    >
    </div>
  </div>
  <div class="relative z-10">
    <PrismaticSphere />
  </div>
</section>

<!-- Matrix Rain Section -->
<section
  id="matrix-rain"
  class="relative bg-gradient-to-b from-black via-green-950/20 to-neutral-900 py-32"
>
  <div class="absolute inset-0">
    <div
      class="absolute inset-0 bg-[radial-gradient(circle_at_center,rgba(34,197,94,0.1),transparent_70%)]"
    >
    </div>
  </div>
  <div class="relative z-10">
    <MatrixRain />
  </div>
</section>

<!-- Interactive Demo Section -->
<section class="relative py-32">
  <div class="container mx-auto px-6">
    <RevealOnScroll animation="slide-up">
      <h2 class="mb-6 text-center text-6xl font-bold text-gradient">
        Interactive Demonstrations
      </h2>
      <p class="mx-auto mb-20 max-w-2xl text-center text-xl text-neutral-400">
        Click, hover, and interact with these live examples
      </p>
    </RevealOnScroll>

    <div class="mx-auto grid max-w-6xl gap-12 md:grid-cols-2">
      <!-- Morphing Card Demo -->
      <RevealOnScroll animation="slide-right" delay={100}>
        <ModernCard variant="glass" className="interactive-demo-card p-8">
          <h3 class="mb-4 text-3xl font-bold text-gradient-blue">
            Morphing Shapes
          </h3>
          <p class="mb-6 text-neutral-400">
            Organic blob animations with smooth transitions
          </p>
          <div
            class="demo-container relative flex h-64 items-center justify-center"
          >
            <div class="morphing-blob-1"></div>
            <div class="morphing-blob-2"></div>
            <div class="morphing-blob-3"></div>
          </div>
        </ModernCard>
      </RevealOnScroll>

      <!-- Particle System Demo -->
      <RevealOnScroll animation="slide-left" delay={200}>
        <ModernCard variant="glass" className="interactive-demo-card p-8">
          <h3 class="mb-4 text-3xl font-bold text-gradient-purple">
            Particle System
          </h3>
          <p class="mb-6 text-neutral-400">
            Dynamic particle generation and physics
          </p>
          <div
            class="demo-container particle-demo relative flex h-64 items-center justify-center overflow-hidden"
          >
            <!-- Particles added via JavaScript -->
          </div>
        </ModernCard>
      </RevealOnScroll>

      <!-- Wave Animation Demo -->
      <RevealOnScroll animation="slide-right" delay={300}>
        <ModernCard variant="glass" className="interactive-demo-card p-8">
          <h3 class="text-shine mb-4 text-3xl font-bold">Wave Distortion</h3>
          <p class="mb-6 text-neutral-400">
            Sin wave patterns with fluid motion
          </p>
          <div class="demo-container flex h-64 items-center justify-center">
            <svg viewBox="0 0 400 200" class="h-full w-full">
              {
                Array.from({ length: 8 }).map((_, i) => (
                  <path
                    class={`wave-path wave-${i + 1}`}
                    d="M0,100 Q50,50 100,100 T200,100 T300,100 T400,100"
                    fill="none"
                    stroke={`rgba(${99 + i * 20}, ${102 + i * 10}, 241, 0.${8 - i})`}
                    stroke-width="2"
                    style={`animation-delay: ${i * 0.1}s`}
                  />
                ))
              }
            </svg>
          </div>
        </ModernCard>
      </RevealOnScroll>

      <!-- Gradient Mesh Demo -->
      <RevealOnScroll animation="slide-left" delay={400}>
        <ModernCard variant="glass" className="interactive-demo-card p-8">
          <h3 class="mb-4 text-3xl font-bold text-gradient">Color Mesh</h3>
          <p class="mb-6 text-neutral-400">Dynamic gradient mesh animations</p>
          <div
            class="demo-container relative flex h-64 items-center justify-center overflow-hidden"
          >
            <div class="gradient-orb orb-1"></div>
            <div class="gradient-orb orb-2"></div>
            <div class="gradient-orb orb-3"></div>
            <div class="gradient-orb orb-4"></div>
          </div>
        </ModernCard>
      </RevealOnScroll>
    </div>
  </div>
</section>

<!-- Typography Showcase -->
<section class="relative bg-neutral-900/50 py-32">
  <div class="container mx-auto px-6">
    <div class="mx-auto max-w-5xl space-y-20">
      <RevealOnScroll animation="fade">
        <div class="text-center">
          <h2
            class="mb-4 animate-gradient bg-[length:200%_200%] text-8xl font-bold text-gradient md:text-9xl"
          >
            ANIMATED
          </h2>
          <p class="text-xl text-neutral-400">
            Smooth gradient color transitions
          </p>
        </div>
      </RevealOnScroll>

      <RevealOnScroll animation="fade" delay={200}>
        <div class="text-center">
          <h2
            class="text-shine mb-4 animate-shimmer bg-gradient-to-r from-white via-purple-300 to-white bg-[length:200%_100%] bg-clip-text text-8xl font-bold text-transparent md:text-9xl"
          >
            SHIMMER
          </h2>
          <p class="text-xl text-neutral-400">Metallic light sweep effect</p>
        </div>
      </RevealOnScroll>

      <RevealOnScroll animation="fade" delay={400}>
        <div class="text-center">
          <h2
            class="glow-strong mb-4 bg-gradient-to-r from-cyan-400 via-blue-500 to-purple-600 bg-clip-text text-8xl font-bold text-transparent md:text-9xl"
          >
            LUMINOUS
          </h2>
          <p class="text-xl text-neutral-400">Glowing neon-style text</p>
        </div>
      </RevealOnScroll>
    </div>
  </div>
</section>

<!-- Interactive Cards Grid -->
<section class="relative py-32">
  <div class="container mx-auto px-6">
    <RevealOnScroll animation="slide-up">
      <h2 class="mb-6 text-center text-6xl font-bold text-gradient-purple">
        Micro-Interactions
      </h2>
      <p class="mx-auto mb-20 max-w-2xl text-center text-xl text-neutral-400">
        Hover and click to see responsive animations
      </p>
    </RevealOnScroll>

    <div class="mx-auto grid max-w-6xl grid-cols-2 gap-6 md:grid-cols-4">
      {
        Array.from({ length: 8 }).map((_, i) => (
          <RevealOnScroll animation="scale" delay={i * 50}>
            <ModernCard
              variant="glass"
              className="interactive-tile aspect-square cursor-pointer group hover:scale-110 transition-all duration-500 hover:rotate-3 hover:shadow-2xl relative overflow-hidden"
            >
              <div class="absolute inset-0 bg-gradient-to-br from-purple-500/20 to-pink-500/20 opacity-0 transition-opacity duration-500 group-hover:opacity-100" />
              <div class="absolute inset-0 flex items-center justify-center">
                <span class="drop-shadow-glow text-6xl filter transition-transform duration-500 group-hover:scale-125">
                  {['⚡', '🌟', '🎯', '🔮', '💎', '🎨', '✨', '🚀'][i]}
                </span>
              </div>
              <div class="absolute inset-0 opacity-5 pattern-dots" />
            </ModernCard>
          </RevealOnScroll>
        ))
      }
    </div>
  </div>
</section>

<!-- Button Showcase -->
<section
  class="relative bg-gradient-to-b from-transparent via-purple-950/20 to-transparent py-32"
>
  <div class="container mx-auto px-6">
    <RevealOnScroll animation="slide-up">
      <h2 class="mb-6 text-center text-6xl font-bold text-gradient">
        Advanced Buttons
      </h2>
      <p class="mx-auto mb-20 max-w-2xl text-center text-xl text-neutral-400">
        Each features ripple effects, shimmer animations, and smooth transitions
      </p>
    </RevealOnScroll>

    <div class="mx-auto max-w-4xl space-y-12">
      <div class="flex flex-wrap items-center justify-center gap-6">
        <ModernButton variant="primary" size="lg" shadow="xl"
          >Primary</ModernButton
        >
        <ModernButton variant="gradient" size="lg" shadow="xl"
          >Gradient</ModernButton
        >
        <ModernButton variant="neon" size="lg" shadow="xl"
          >Neon Glow</ModernButton
        >
        <ModernButton variant="outline" size="lg">Outlined</ModernButton>
      </div>

      <div class="flex flex-wrap items-center justify-center gap-6">
        <ModernButton variant="success" size="md" shadow="lg"
          >Success</ModernButton
        >
        <ModernButton variant="warning" size="md" shadow="lg"
          >Warning</ModernButton
        >
        <ModernButton variant="destructive" size="md" shadow="lg"
          >Danger</ModernButton
        >
        <ModernButton variant="ghost" size="md">Ghost</ModernButton>
      </div>
    </div>
  </div>
</section>

<!-- Final CTA -->
<section class="relative py-32">
  <div class="container mx-auto px-6">
    <RevealOnScroll animation="scale">
      <ModernCard
        variant="glass"
        className="mx-auto max-w-4xl p-16 text-center"
        glow
      >
        <h2 class="mb-6 text-5xl font-bold text-gradient-purple md:text-6xl">
          Ready to Build Something Amazing?
        </h2>
        <p class="mx-auto mb-12 max-w-2xl text-xl text-neutral-400">
          These demonstrations showcase what's possible with modern web
          technologies. Every element is crafted for performance and visual
          excellence.
        </p>
        <div class="flex flex-wrap justify-center gap-6">
          <ModernButton variant="neon" size="xl" shadow="xl">
            Get Started
          </ModernButton>
          <ModernButton variant="outline" size="xl">
            View Documentation
          </ModernButton>
        </div>
      </ModernCard>
    </RevealOnScroll>
  </div>
</section>

<style>
  /* Defer rendering of heavy sections until visible */
  main > section {
    content-visibility: auto;
    contain: layout paint style;
    contain-intrinsic-size: 900px;
  }

  /* Morphing blobs */
  .morphing-blob-1,
  .morphing-blob-2,
  .morphing-blob-3 {
    position: absolute;
    border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;
    animation:
      morph 8s ease-in-out infinite,
      float 6s ease-in-out infinite;
    filter: blur(40px);
  }

  .morphing-blob-1 {
    width: 200px;
    height: 200px;
    background: linear-gradient(
      135deg,
      rgba(99, 102, 241, 0.6),
      rgba(168, 85, 247, 0.6)
    );
  }

  .morphing-blob-2 {
    width: 180px;
    height: 180px;
    background: linear-gradient(
      135deg,
      rgba(236, 72, 153, 0.5),
      rgba(239, 68, 68, 0.5)
    );
    animation-delay: -2s;
  }

  .morphing-blob-3 {
    width: 160px;
    height: 160px;
    background: linear-gradient(
      135deg,
      rgba(59, 130, 246, 0.5),
      rgba(16, 185, 129, 0.5)
    );
    animation-delay: -4s;
  }

  @keyframes morph {
    0%,
    100% {
      border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;
    }
    25% {
      border-radius: 58% 42% 75% 25% / 76% 46% 54% 24%;
    }
    50% {
      border-radius: 50% 50% 33% 67% / 55% 27% 73% 45%;
    }
    75% {
      border-radius: 33% 67% 58% 42% / 63% 68% 32% 37%;
    }
  }

  /* Wave animations */
  .wave-path {
    animation: wave-flow 3s ease-in-out infinite;
  }

  @keyframes wave-flow {
    0%,
    100% {
      d: path('M0,100 Q50,50 100,100 T200,100 T300,100 T400,100');
    }
    50% {
      d: path('M0,100 Q50,150 100,100 T200,100 T300,100 T400,100');
    }
  }

  /* Gradient orbs */
  .gradient-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(60px);
    animation: float-orb 8s ease-in-out infinite;
    mix-blend-mode: screen;
  }

  .orb-1 {
    width: 150px;
    height: 150px;
    background: rgba(99, 102, 241, 0.7);
    top: 20%;
    left: 20%;
  }

  .orb-2 {
    width: 120px;
    height: 120px;
    background: rgba(236, 72, 153, 0.7);
    top: 60%;
    right: 20%;
    animation-delay: -2s;
  }

  .orb-3 {
    width: 100px;
    height: 100px;
    background: rgba(59, 130, 246, 0.7);
    bottom: 20%;
    left: 30%;
    animation-delay: -4s;
  }

  .orb-4 {
    width: 130px;
    height: 130px;
    background: rgba(168, 85, 247, 0.7);
    top: 40%;
    right: 40%;
    animation-delay: -6s;
  }

  @keyframes float-orb {
    0%,
    100% {
      transform: translate(0, 0) scale(1);
    }
    33% {
      transform: translate(30px, -30px) scale(1.1);
    }
    66% {
      transform: translate(-20px, 20px) scale(0.9);
    }
  }

  /* Interactive tile hover effect */
  .interactive-tile::before {
    content: '';
    position: absolute;
    inset: -2px;
    background: linear-gradient(45deg, #6366f1, #a855f7, #ec4899, #6366f1);
    background-size: 300% 300%;
    border-radius: inherit;
    opacity: 0;
    transition: opacity 0.5s;
    animation: gradient-rotate 4s linear infinite;
    z-index: -1;
  }

  .interactive-tile:hover::before {
    opacity: 1;
  }

  @keyframes gradient-rotate {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }

  /* Demo card styles */
  .interactive-demo-card {
    transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .interactive-demo-card:hover {
    transform: translateY(-8px) scale(1.02);
    box-shadow: 0 20px 60px rgba(168, 85, 247, 0.3);
  }

  @media (prefers-reduced-motion: reduce) {
    .morphing-blob-1,
    .morphing-blob-2,
    .morphing-blob-3,
    .interactive-tile::before,
    .interactive-demo-card,
    .particle-generated {
      animation: none !important;
      transition: none !important;
    }
    .interactive-demo-card:hover {
      transform: none;
      box-shadow: none;
    }
  }
</style>

-->
