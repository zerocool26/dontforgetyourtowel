---
// Legacy blog route intentionally disabled for the MSP site.
// Returning an empty list ensures Astro generates no /blog/:slug pages in static output.

export const prerender = true;

export async function getStaticPaths() {
  return [];
}
---
