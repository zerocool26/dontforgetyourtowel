---
// Legacy blog route intentionally disabled for the MSP site.
// Returning an empty list ensures Astro generates no /blog/tag/:tag pages in static output.

export async function getStaticPaths() {
  return [];
}
---
