---
import ModernCard from '../ui/ModernCard.astro';
import ModernButton from '../ui/ModernButton.astro';

export interface Props {
  title: string;
  description: string;
  price?: string;
  priceNote?: string;
  href?: string;
  bullets?: string[];
}

const {
  title,
  description,
  price,
  priceNote,
  href,
  bullets = [],
} = Astro.props as Props;
---

<ModernCard
  variant="minimal"
  className="creative-panel tone-elevated tone-border h-full border p-6 transition-colors hover:border-accent-500/50"
>
  <div class="flex items-start justify-between gap-6">
    <div class="space-y-2">
      <h3 class="tone-title text-lg font-semibold">{title}</h3>
      <p class="tone-body text-sm leading-relaxed">{description}</p>
    </div>
    {
      price && (
        <div class="text-right">
          <p class="tone-title text-sm font-semibold">{price}</p>
          {priceNote && <p class="tone-muted text-[0.75rem]">{priceNote}</p>}
        </div>
      )
    }
  </div>

  {
    bullets.length > 0 && (
      <ul class="tone-body mt-5 space-y-2 text-sm">
        {bullets.map(item => (
          <li class="flex items-start gap-2">
            <span class="mt-2 h-1.5 w-1.5 flex-shrink-0 bg-accent-500" />
            <span>{item}</span>
          </li>
        ))}
      </ul>
    )
  }

  {
    href && (
      <div class="mt-6">
        <ModernButton
          href={href}
          variant="secondary"
          className="w-full min-h-[48px]"
        >
          Learn more
        </ModernButton>
      </div>
    )
  }
</ModernCard>
