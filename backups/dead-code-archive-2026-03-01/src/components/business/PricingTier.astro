---
import ModernCard from '../ui/ModernCard.astro';
import ModernButton from '../ui/ModernButton.astro';

export interface Props {
  name: string;
  price: string;
  description: string;
  perks: string[];
  highlight?: boolean;
  ctaLabel: string;
  ctaHref: string;
}

const {
  name,
  price,
  description,
  perks,
  highlight = false,
  ctaLabel,
  ctaHref,
} = Astro.props as Props;
---

<ModernCard
  variant="minimal"
  className={`flex h-full flex-col gap-6 p-7 ${highlight ? 'border-[#ccff00] bg-[#030303]' : 'border-white/10 bg-white/5 hover:border-white/20'} transition-colors`}
>
  <div class="space-y-3">
    <span
      class={`inline-flex items-center gap-2 rounded-full px-3 py-1 text-xs font-semibold uppercase tracking-[0.25em] ${highlight ? 'bg-[#ccff00]/20 text-[#ccff00]' : 'bg-white/5 text-neutral-400'}`}
    >
      {highlight ? 'Recommended' : 'Tier'}
    </span>
    <h3 class="text-2xl font-semibold text-white">{name}</h3>
    <p class="text-3xl font-semibold text-white">{price}</p>
    <p class="text-sm text-neutral-400">{description}</p>
  </div>

  <ul class="space-y-2 text-sm text-neutral-400">
    {
      perks.map(perk => (
        <li class="flex items-start gap-2">
          <span class="mt-2 h-1.5 w-1.5 flex-shrink-0 bg-[#ccff00]" />
          <span>{perk}</span>
        </li>
      ))
    }
  </ul>

  <div class="mt-auto">
    <ModernButton
      href={ctaHref}
      variant={highlight ? 'primary' : 'secondary'}
      className="min-h-[48px] w-full"
    >
      {ctaLabel}
    </ModernButton>
  </div>
</ModernCard>
