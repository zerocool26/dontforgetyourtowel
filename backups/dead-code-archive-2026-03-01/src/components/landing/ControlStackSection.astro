---
import { controlStack } from '../../data/marketing';

const sectionId = 'control-stack-section';
---

<section class="relative" aria-labelledby={`${sectionId}-title`}>
  <div
    class="absolute inset-0 -z-10 bg-[rgba(8,13,24,0.78)]"
    aria-hidden="true"
  >
  </div>
  <div class="relative mx-auto max-w-[1220px] px-6 py-24">
    <div class="grid gap-12 lg:grid-cols-[minmax(0,0.85fr)_minmax(0,1.15fr)]">
      <header class="space-y-4">
        <p
          class="text-xs font-semibold uppercase tracking-[0.32em] text-slate-400"
          aria-hidden="true"
        >
          Control stack
        </p>
        <h2
          id={`${sectionId}-title`}
          class="text-3xl font-semibold text-white sm:text-4xl"
        >
          Deploy a consistent operating layer across every release
        </h2>
        <p class="text-sm text-slate-300">
          Each module is designed to drop into your existing workflow,
          connecting runbooks and audit trails with calm, executive-ready
          interfaces.
        </p>
      </header>
      <div
        class="grid gap-6 lg:grid-cols-3"
        role="list"
        aria-label="Control stack modules"
      >
        {
          controlStack.map((stack, index) => (
            <article
              class="glass-panel flex h-full flex-col gap-5 border border-white/10 p-6 transition-all duration-300 focus-within:ring-2 focus-within:ring-indigo-500/50 hover:border-indigo-500/30 hover:shadow-lg"
              role="listitem"
              aria-labelledby={`stack-${index}-title`}
            >
              <div class="flex items-start justify-between gap-4">
                <h3
                  id={`stack-${index}-title`}
                  class="text-lg font-semibold text-white"
                >
                  {stack.name}
                </h3>
                <div class="text-right">
                  <p
                    class="text-sm font-semibold tabular-nums text-white"
                    aria-label={`${stack.metricLabel}: ${stack.metric}`}
                  >
                    {stack.metric}
                  </p>
                  <p
                    class="text-[0.65rem] uppercase tracking-[0.3em] text-slate-400"
                    aria-hidden="true"
                  >
                    {stack.metricLabel}
                  </p>
                </div>
              </div>
              <p class="text-sm text-slate-300">{stack.summary}</p>
              <ul
                class="space-y-2 text-sm text-slate-300/90"
                aria-label={`Features for ${stack.name}`}
              >
                {stack.bullets.map(point => (
                  <li class="flex items-start gap-2">
                    <span
                      class="mt-1 inline-flex h-2 w-2 flex-shrink-0 rounded-full bg-[rgba(99,102,241,0.55)]"
                      aria-hidden="true"
                    />
                    <span>{point}</span>
                  </li>
                ))}
              </ul>
            </article>
          ))
        }
      </div>
    </div>
  </div>
</section>

<style>
  .tabular-nums {
    font-variant-numeric: tabular-nums;
  }
</style>
